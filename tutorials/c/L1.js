/* c-L1.js — C Lesson 1 content (EN + TR) */
var C_L1 = {
en: "<p class=\"l-text\">Welcome to C — the language that powers the modern world. Created by Dennis Ritchie at Bell Labs in 1972, C was designed to build Unix, and it has since become the backbone of nearly every operating system, embedded device, and performance-critical application you use today.</p><div class=\"lesson-outcomes\" style=\"background:rgba(200,169,110,0.06);border-left:3px solid #c8a96e;padding:1rem 1.2rem;margin:1.5rem 0;border-radius:0 8px 8px 0\"><div style=\"font-size:0.78rem;font-weight:700;letter-spacing:0.08em;color:#c8a96e;margin-bottom:0.6rem\">📍 WHAT YOU'LL LEARN</div><ul style=\"margin:0;padding-left:1.3rem;line-height:1.65;font-size:0.93rem;color:rgba(235,230,220,0.92)\"><li>Compile and run a C program through the four-stage GCC pipeline</li><li>Read main, printf, return values, and the structure of a C source file</li><li>Use argc and argv to accept command-line arguments in your programs</li><li>Enable -Wall -Wextra warnings and treat them as actual bugs</li><li>Avoid common beginner pitfalls like missing semicolons and = vs ==</li></ul></div><div class=\"l-highlight\"><strong>Why learn C in 2026?</strong> The Linux kernel, Windows kernel, macOS kernel, PostgreSQL, Redis, Git, Python\\u0027s interpreter, and nearly every embedded system on Earth are written in C. When you understand C, you understand how computers actually work — not through abstractions, but directly.</div><p class=\"l-text\">Here is what C gives you that higher-level languages hide:</p><ul class=\"l-list\"><li><strong>Direct memory access</strong> — you decide exactly where data lives and when it gets freed</li><li><strong>Zero-cost abstractions</strong> — no garbage collector, no runtime overhead, no hidden allocations</li><li><strong>Hardware proximity</strong> — you can talk to registers, write device drivers, build operating systems</li><li><strong>Portability</strong> — C compilers exist for virtually every CPU architecture ever made</li><li><strong>Foundation for everything</strong> — C++, Rust, Go, and even Python\\u0027s internals are deeply influenced by C</li></ul><p class=\"l-text\">Let\\u0027s start with the simplest possible C program and understand every single line:</p><div class=\"code-wrap\"><div class=\"code-label\"><span>C</span> hello.c <button class=\"code-copy\">COPY</button></div><div class=\"code-block\"><pre><code><span class=\"pp\">#include</span> <span class=\"str\">&lt;stdio.h&gt;</span>    <span class=\"cm\">/* standard input/output header */</span>\n\n<span class=\"ty\">int</span> <span class=\"fn\">main</span>(<span class=\"ty\">void</span>) {\n    <span class=\"fn\">printf</span>(<span class=\"str\">\"Hello, World!\\n\"</span>);\n    <span class=\"kw\">return</span> <span class=\"num\">0</span>;\n}</code></pre></div></div><p class=\"l-text\">Let\\u0027s break down every line:</p><ul class=\"l-list\"><li><code>#include &lt;stdio.h&gt;</code> — This is a <strong>preprocessor directive</strong>. Before compilation even starts, the preprocessor copies the entire contents of <code>stdio.h</code> (standard I/O header) into your file. This header declares functions like <code>printf</code>, <code>scanf</code>, <code>fopen</code>, etc. Without it, the compiler does not know what <code>printf</code> is.</li><li><code>int main(void)</code> — Every C program must have exactly one <code>main</code> function. This is where execution begins. The <code>int</code> return type means it returns an integer to the operating system. <code>void</code> means it takes no arguments.</li><li><code>printf(\"Hello, World!\\n\")</code> — Prints the string to standard output (your terminal). The <code>\\n</code> is a newline character — without it, your terminal prompt would appear on the same line as the output.</li><li><code>return 0</code> — Returns 0 to the OS, which means \"success.\" Any non-zero value signals an error. You can check this with <code>echo $?</code> in your terminal after running the program.</li></ul><div class=\"code-wrap\"><div class=\"code-label\"><span>BASH</span> Compile &amp; Run <button class=\"code-copy\">COPY</button></div><div class=\"code-block\"><pre><code><span class=\"cm\"># Basic compile: source file → executable</span>\ngcc hello.c -o hello\n./hello\n\n<span class=\"cm\"># Recommended: enable all warnings + C11 standard</span>\ngcc -Wall -Wextra -std=c11 hello.c -o hello\n\n<span class=\"cm\"># With debug symbols (needed for gdb/lldb debugger)</span>\ngcc -g -Wall -Wextra -std=c11 hello.c -o hello\n\n<span class=\"cm\"># Check the exit code</span>\necho $?</code></pre></div></div><div class=\"code-wrap\"><div class=\"code-label\"><span>OUTPUT</span></div><div class=\"code-block output-block\"><pre><code>Hello, World!</code></pre></div></div><div class=\"l-highlight\"><strong>The Compilation Pipeline</strong> — When you run <code>gcc hello.c -o hello</code>, four separate stages happen behind the scenes. Understanding this pipeline is crucial because errors at different stages have different causes and solutions.</div><p class=\"l-text\">The four stages of compilation:</p><ul class=\"l-list\"><li><strong>1. Preprocessing</strong> (<code>gcc -E hello.c -o hello.i</code>) — The preprocessor handles all lines starting with <code>#</code>. It expands <code>#include</code> by copying header file contents, replaces <code>#define</code> macros with their values, and processes conditional compilation (<code>#ifdef</code>). The output is a pure C file with no preprocessor directives, often thousands of lines long.</li><li><strong>2. Compilation</strong> (<code>gcc -S hello.i -o hello.s</code>) — The compiler translates your C code into assembly language for your specific CPU architecture (x86_64, ARM, etc.). This is where syntax errors and type errors are caught. The output is human-readable assembly.</li><li><strong>3. Assembly</strong> (<code>gcc -c hello.s -o hello.o</code>) — The assembler converts assembly instructions into machine code — actual binary instructions your CPU can execute. The output is an object file (<code>.o</code>), but it\\u0027s not yet runnable because function calls to external libraries are unresolved.</li><li><strong>4. Linking</strong> (<code>gcc hello.o -o hello</code>) — The linker resolves all external references. When your code calls <code>printf</code>, the linker finds the actual implementation in the C standard library (<code>libc</code>) and connects them. The output is your final executable.</li></ul><div class=\"code-wrap\"><div class=\"code-label\"><span>BASH</span> See each stage separately <button class=\"code-copy\">COPY</button></div><div class=\"code-block\"><pre><code><span class=\"cm\"># Stage 1: Preprocessing only</span>\ngcc -E hello.c -o hello.i     <span class=\"cm\"># inspect: thousands of lines</span>\n\n<span class=\"cm\"># Stage 2: Compile to assembly</span>\ngcc -S hello.c -o hello.s     <span class=\"cm\"># inspect: human-readable asm</span>\n\n<span class=\"cm\"># Stage 3: Assemble to object file</span>\ngcc -c hello.c -o hello.o     <span class=\"cm\"># binary, not yet executable</span>\n\n<span class=\"cm\"># Stage 4: Link into final executable</span>\ngcc hello.o -o hello           <span class=\"cm\"># now it runs!</span></code></pre></div></div><div class=\"l-note\"><strong>Tip:</strong> Always compile with <code>-Wall -Wextra</code>. These flags enable most compiler warnings. Warnings are not suggestions — they are bugs waiting to happen. Professional C developers treat warnings as errors using <code>-Werror</code>.</div><p class=\"l-text\">Now let\\u0027s look at <strong>command-line arguments</strong>. Most real programs accept input from the command line. When you type <code>gcc -Wall hello.c -o hello</code>, the words <code>gcc</code>, <code>-Wall</code>, <code>hello.c</code>, <code>-o</code>, and <code>hello</code> are all arguments passed to the gcc program.</p><p class=\"l-text\">Your programs can accept arguments too, using the alternative <code>main</code> signature:</p><div class=\"code-wrap\"><div class=\"code-label\"><span>C</span> args.c <button class=\"code-copy\">COPY</button></div><div class=\"code-block\"><pre><code><span class=\"pp\">#include</span> <span class=\"str\">&lt;stdio.h&gt;</span>\n\n<span class=\"ty\">int</span> <span class=\"fn\">main</span>(<span class=\"ty\">int</span> argc, <span class=\"ty\">char</span> *argv[]) {\n    <span class=\"fn\">printf</span>(<span class=\"str\">\"Number of arguments: %d\\n\"</span>, argc);\n    <span class=\"kw\">for</span> (<span class=\"ty\">int</span> i = <span class=\"num\">0</span>; i &lt; argc; i++) {\n        <span class=\"fn\">printf</span>(<span class=\"str\">\"  argv[%d] = \\\"%s\\\"\\n\"</span>, i, argv[i]);\n    }\n    <span class=\"kw\">return</span> <span class=\"num\">0</span>;\n}</code></pre></div></div><div class=\"code-wrap\"><div class=\"code-label\"><span>BASH</span> Compile &amp; Run <button class=\"code-copy\">COPY</button></div><div class=\"code-block\"><pre><code>gcc -Wall -Wextra -std=c11 args.c -o args\n./args hello world 42</code></pre></div></div><div class=\"code-wrap\"><div class=\"code-label\"><span>OUTPUT</span></div><div class=\"code-block output-block\"><pre><code>Number of arguments: 4\n  argv[0] = \"./args\"\n  argv[1] = \"hello\"\n  argv[2] = \"world\"\n  argv[3] = \"42\"</code></pre></div></div><p class=\"l-text\">Key points about <code>argc</code> and <code>argv</code>:</p><ul class=\"l-list\"><li><code>argc</code> (argument count) — the total number of arguments, including the program name itself</li><li><code>argv</code> (argument vector) — an array of strings. <code>argv[0]</code> is always the program name</li><li>All arguments are strings. If you need a number, you must convert it: <code>int n = atoi(argv[1])</code></li></ul><p class=\"l-text\">Here is a practical example — a program that greets the user by name:</p><div class=\"code-wrap\"><div class=\"code-label\"><span>C</span> greet.c <button class=\"code-copy\">COPY</button></div><div class=\"code-block\"><pre><code><span class=\"pp\">#include</span> <span class=\"str\">&lt;stdio.h&gt;</span>\n\n<span class=\"ty\">int</span> <span class=\"fn\">main</span>(<span class=\"ty\">int</span> argc, <span class=\"ty\">char</span> *argv[]) {\n    <span class=\"kw\">if</span> (argc &lt; <span class=\"num\">2</span>) {\n        <span class=\"fn\">fprintf</span>(<span class=\"pp\">stderr</span>, <span class=\"str\">\"Usage: %s &lt;name&gt;\\n\"</span>, argv[<span class=\"num\">0</span>]);\n        <span class=\"kw\">return</span> <span class=\"num\">1</span>;    <span class=\"cm\">/* non-zero = error */</span>\n    }\n    <span class=\"fn\">printf</span>(<span class=\"str\">\"Hello, %s! Welcome to C.\\n\"</span>, argv[<span class=\"num\">1</span>]);\n    <span class=\"kw\">return</span> <span class=\"num\">0</span>;\n}</code></pre></div></div><div class=\"code-wrap\"><div class=\"code-label\"><span>BASH</span> Compile &amp; Run <button class=\"code-copy\">COPY</button></div><div class=\"code-block\"><pre><code>gcc -Wall -Wextra -std=c11 greet.c -o greet\n./greet Mikail\n./greet</code></pre></div></div><div class=\"code-wrap\"><div class=\"code-label\"><span>OUTPUT</span></div><div class=\"code-block output-block\"><pre><code>Hello, Mikail! Welcome to C.\nUsage: ./greet &lt;name&gt;</code></pre></div></div><p class=\"l-text\">Notice: we use <code>fprintf(stderr, ...)</code> for error messages instead of <code>printf</code>. This writes to the error stream, which can be redirected separately from normal output. The return value of <code>1</code> tells the OS (and shell scripts) that something went wrong.</p><div class=\"l-warn\"><strong>Common beginner mistakes:</strong></div><ul class=\"l-list\"><li><strong>Forgetting <code>#include &lt;stdio.h&gt;</code></strong> — you\\u0027ll get \"implicit declaration of function printf\" warning</li><li><strong>Missing semicolons</strong> — C requires a <code>;</code> at the end of every statement. The error often points to the next line, not the line where the semicolon is missing.</li><li><strong>Using <code>=</code> instead of <code>==</code></strong> — <code>=</code> is assignment, <code>==</code> is comparison. <code>if (x = 5)</code> assigns 5 to x and always evaluates to true.</li><li><strong>Not checking compiler warnings</strong> — if you compile without <code>-Wall</code>, the compiler silently ignores potential bugs</li><li><strong>Naming your file <code>test.c</code></strong> — on many systems, <code>test</code> is a built-in command. Name it something specific like <code>mytest.c</code>.</li></ul>",
tr: "<p class=\"l-text\">C'ye hoş geldiniz — modern dünyayı harekete geçiren dil. Dennis Ritchie tarafından 1972'de Bell Labs'ta oluşturulan C, Unix'i inşa etmek için tasarlanmıştı ve o zamandan beri bugün kullandığınız neredeyse her işletim sistemi, gömülü cihaz ve performans-kritik uygulamanın temelini oluşturmaktadır.</p><div class=\"lesson-outcomes\" style=\"background:rgba(200,169,110,0.06);border-left:3px solid #c8a96e;padding:1rem 1.2rem;margin:1.5rem 0;border-radius:0 8px 8px 0\"><div style=\"font-size:0.78rem;font-weight:700;letter-spacing:0.08em;color:#c8a96e;margin-bottom:0.6rem\">📍 BU DERSTE ÖĞRENECEKSİN</div><ul style=\"margin:0;padding-left:1.3rem;line-height:1.65;font-size:0.93rem;color:rgba(235,230,220,0.92)\"><li>Bir C programını dört aşamalı GCC pipeline'ı ile derle ve çalıştır</li><li>main, printf, return değerleri ve bir C kaynak dosyasının yapısını oku</li><li>Programlarında komut satırı argümanı kabul etmek için argc ve argv kullan</li><li>-Wall -Wextra uyarılarını etkinleştir ve onları gerçek hata olarak işle</li><li>Eksik noktalı virgül ve = vs == gibi yaygın başlangıç tuzaklarından kaçın</li></ul></div><div class=\"l-highlight\"><strong>2026'da neden C öğrenmelisiniz?</strong> Linux çekirdeği, Windows çekirdeği, macOS çekirdeği, PostgreSQL, Redis, Git, Python'ın yorumlayıcısı ve dünya üzerindeki neredeyse her gömülü sistem C ile yazılmıştır. C'yi anladığınızda, bilgisayarların gerçekte nasıl çalıştığını anlıyorsunuz — soyutlamalar aracılığıyla değil, doğrudan.</div><p class=\"l-text\">Üst düzey dillerin sizden gizlediği, C'nin size sunduğu şeyler:</p><ul class=\"l-list\"><li><strong>Doğrudan bellek erişimi</strong> — verinin tam olarak nerede yaşayacağına ve ne zaman serbest bırakılacağına siz karar verirsiniz</li><li><strong>Sıfır maliyetli soyutlamalar</strong> — çöp toplayıcı yok, çalışma zamanı yükü yok, gizli bellek tahsisleri yok</li><li><strong>Donanıma yakınlık</strong> — registerlara erişebilir, aygıt sürücüleri yazabilir, işletim sistemleri inşa edebilirsiniz</li><li><strong>Taşınabilirlik</strong> — C derleyicileri şimdiye kadar üretilmiş neredeyse her CPU mimarisi için mevcuttur</li><li><strong>Her şeyin temeli</strong> — C++, Rust, Go ve hatta Python'ın iç yapısı C'den derinden etkilenmiştir</li></ul><p class=\"l-text\">Mümkün olan en basit C programıyla başlayalım ve her satırı tek tek anlayalım:</p><div class=\"code-wrap\"><div class=\"code-label\"><span>C</span> hello.c <button class=\"code-copy\">COPY</button></div><div class=\"code-block\"><pre><code><span class=\"pp\">#include</span> <span class=\"str\">&lt;stdio.h&gt;</span>    <span class=\"cm\">/* standart girdi/çıktı başlık dosyası */</span>\n\n<span class=\"ty\">int</span> <span class=\"fn\">main</span>(<span class=\"ty\">void</span>) {\n    <span class=\"fn\">printf</span>(<span class=\"str\">\"Hello, World!\\n\"</span>);\n    <span class=\"kw\">return</span> <span class=\"num\">0</span>;\n}</code></pre></div></div><p class=\"l-text\">Her satırı inceleyelim:</p><ul class=\"l-list\"><li><code>#include &lt;stdio.h&gt;</code> — Bu bir <strong>önişlemci yönergesi</strong>dir. Derleme başlamadan önce, önişlemci <code>stdio.h</code> (standart G/Ç başlık dosyası) dosyasının tüm içeriğini dosyanıza kopyalar. Bu başlık dosyası <code>printf</code>, <code>scanf</code>, <code>fopen</code> gibi fonksiyonları bildirir. Bu olmadan derleyici <code>printf</code>'in ne olduğunu bilmez.</li><li><code>int main(void)</code> — Her C programının tam olarak bir <code>main</code> fonksiyonu olmalıdır. Yürütme buradan başlar. <code>int</code> dönüş tipi, işletim sistemine bir tam sayı döndürdüğü anlamına gelir. <code>void</code> ise hiçbir argüman almadığını belirtir.</li><li><code>printf(\"Hello, World!\\n\")</code> — Dizeyi standart çıktıya (terminalinize) yazdırır. <code>\\n</code> yeni satır karakteridir — bu olmadan terminal isteminiz çıktıyla aynı satırda görünür.</li><li><code>return 0</code> — İşletim sistemine 0 döndürür, bu \"başarılı\" anlamına gelir. Sıfır olmayan herhangi bir değer hata sinyali verir. Programı çalıştırdıktan sonra terminalinizde <code>echo $?</code> ile bunu kontrol edebilirsiniz.</li></ul><div class=\"code-wrap\"><div class=\"code-label\"><span>BASH</span> Derle &amp; Çalıştır <button class=\"code-copy\">COPY</button></div><div class=\"code-block\"><pre><code><span class=\"cm\"># Temel derleme: kaynak dosya → çalıştırılabilir</span>\ngcc hello.c -o hello\n./hello\n\n<span class=\"cm\"># Önerilen: tüm uyarıları etkinleştir + C11 standardı</span>\ngcc -Wall -Wextra -std=c11 hello.c -o hello\n\n<span class=\"cm\"># Hata ayıklama sembolleriyle (gdb/lldb hata ayıklayıcısı için gerekli)</span>\ngcc -g -Wall -Wextra -std=c11 hello.c -o hello\n\n<span class=\"cm\"># Çıkış kodunu kontrol et</span>\necho $?</code></pre></div></div><div class=\"code-wrap\"><div class=\"code-label\"><span>OUTPUT</span></div><div class=\"code-block output-block\"><pre><code>Hello, World!</code></pre></div></div><div class=\"l-highlight\"><strong>Derleme Süreci</strong> — <code>gcc hello.c -o hello</code> komutunu çalıştırdığınızda arka planda dört ayrı aşama gerçekleşir. Bu süreci anlamak çok önemlidir çünkü farklı aşamalardaki hataların farklı nedenleri ve çözümleri vardır.</div><p class=\"l-text\">Derlemenin dört aşaması:</p><ul class=\"l-list\"><li><strong>1. Önişleme</strong> (<code>gcc -E hello.c -o hello.i</code>) — Önişlemci <code>#</code> ile başlayan tüm satırları işler. <code>#include</code> ifadelerini başlık dosyası içeriklerini kopyalayarak genişletir, <code>#define</code> makrolarını değerleriyle değiştirir ve koşullu derlemeyi (<code>#ifdef</code>) işler. Çıktı, önişlemci yönergesi içermeyen saf bir C dosyasıdır, genellikle binlerce satır uzunluğundadır.</li><li><strong>2. Derleme</strong> (<code>gcc -S hello.i -o hello.s</code>) — Derleyici C kodunuzu belirli CPU mimariniz (x86_64, ARM, vb.) için assembly diline çevirir. Sözdizimi hataları ve tip hataları burada yakalır. Çıktı insan tarafından okunabilir assembly'dir.</li><li><strong>3. Assembler</strong> (<code>gcc -c hello.s -o hello.o</code>) — Assembler, assembly komutlarını makine koduna — CPU'nuzun yürütebileceği gerçek ikili komutlara — dönüştürür. Çıktı bir nesne dosyasıdır (<code>.o</code>), ancak harici kütüphanelere yapılan fonksiyon çağrıları henüz çözülmediği için çalıştırılabilir değildir.</li><li><strong>4. Bağlama</strong> (<code>gcc hello.o -o hello</code>) — Bağlayıcı tüm harici referansları çözer. Kodunuz <code>printf</code>'i çağırdığında, bağlayıcı C standart kütüphanesindeki (<code>libc</code>) gerçek uygulamayı bulur ve bağlar. Çıktı son çalıştırılabilir dosyanızdır.</li></ul><div class=\"code-wrap\"><div class=\"code-label\"><span>BASH</span> Her aşamayı ayrı ayrı görün <button class=\"code-copy\">COPY</button></div><div class=\"code-block\"><pre><code><span class=\"cm\"># Aşama 1: Sadece önişleme</span>\ngcc -E hello.c -o hello.i     <span class=\"cm\"># inceleyin: binlerce satır</span>\n\n<span class=\"cm\"># Aşama 2: Assembly'ye derle</span>\ngcc -S hello.c -o hello.s     <span class=\"cm\"># inceleyin: okunabilir assembly</span>\n\n<span class=\"cm\"># Aşama 3: Nesne dosyasına çevir</span>\ngcc -c hello.c -o hello.o     <span class=\"cm\"># ikili, henüz çalıştırılabilir değil</span>\n\n<span class=\"cm\"># Aşama 4: Son çalıştırılabilir dosyaya bağla</span>\ngcc hello.o -o hello           <span class=\"cm\"># artık çalışıyor!</span></code></pre></div></div><div class=\"l-note\"><strong>İpucu:</strong> Her zaman <code>-Wall -Wextra</code> ile derleyin. Bu bayraklar çoğu derleyici uyarısını etkinleştirir. Uyarılar öneri değildir — gerçekleşmeyi bekleyen hatalardır. Profesyonel C geliştiricileri <code>-Werror</code> kullanarak uyarıları hata olarak değerlendirir.</div><p class=\"l-text\">Şimdi <strong>komut satırı argümanlarına</strong> bakalım. Çoğu gerçek program komut satırından girdi alır. <code>gcc -Wall hello.c -o hello</code> yazdığınızda, <code>gcc</code>, <code>-Wall</code>, <code>hello.c</code>, <code>-o</code> ve <code>hello</code> kelimelerin hepsi gcc programına iletilen argümanlardır.</p><p class=\"l-text\">Programlarınız da alternatif <code>main</code> imzasını kullanarak argüman kabul edebilir:</p><div class=\"code-wrap\"><div class=\"code-label\"><span>C</span> args.c <button class=\"code-copy\">COPY</button></div><div class=\"code-block\"><pre><code><span class=\"pp\">#include</span> <span class=\"str\">&lt;stdio.h&gt;</span>\n\n<span class=\"ty\">int</span> <span class=\"fn\">main</span>(<span class=\"ty\">int</span> argc, <span class=\"ty\">char</span> *argv[]) {\n    <span class=\"fn\">printf</span>(<span class=\"str\">\"Number of arguments: %d\\n\"</span>, argc);\n    <span class=\"kw\">for</span> (<span class=\"ty\">int</span> i = <span class=\"num\">0</span>; i &lt; argc; i++) {\n        <span class=\"fn\">printf</span>(<span class=\"str\">\"  argv[%d] = \\\"%s\\\"\\n\"</span>, i, argv[i]);\n    }\n    <span class=\"kw\">return</span> <span class=\"num\">0</span>;\n}</code></pre></div></div><div class=\"code-wrap\"><div class=\"code-label\"><span>BASH</span> Derle &amp; Çalıştır <button class=\"code-copy\">COPY</button></div><div class=\"code-block\"><pre><code>gcc -Wall -Wextra -std=c11 args.c -o args\n./args hello world 42</code></pre></div></div><div class=\"code-wrap\"><div class=\"code-label\"><span>OUTPUT</span></div><div class=\"code-block output-block\"><pre><code>Number of arguments: 4\n  argv[0] = \"./args\"\n  argv[1] = \"hello\"\n  argv[2] = \"world\"\n  argv[3] = \"42\"</code></pre></div></div><p class=\"l-text\"><code>argc</code> ve <code>argv</code> hakkında önemli noktalar:</p><ul class=\"l-list\"><li><code>argc</code> (argüman sayısı) — program adı dahil toplam argüman sayısı</li><li><code>argv</code> (argüman vektörü) — bir dize dizisi. <code>argv[0]</code> her zaman program adıdır</li><li>Tüm argümanlar dizedir. Bir sayıya ihtiyacınız varsa dönüştürmelisiniz: <code>int n = atoi(argv[1])</code></li></ul><p class=\"l-text\">Pratik bir örnek — kullanıcıyı adıyla selamlayan bir program:</p><div class=\"code-wrap\"><div class=\"code-label\"><span>C</span> greet.c <button class=\"code-copy\">COPY</button></div><div class=\"code-block\"><pre><code><span class=\"pp\">#include</span> <span class=\"str\">&lt;stdio.h&gt;</span>\n\n<span class=\"ty\">int</span> <span class=\"fn\">main</span>(<span class=\"ty\">int</span> argc, <span class=\"ty\">char</span> *argv[]) {\n    <span class=\"kw\">if</span> (argc &lt; <span class=\"num\">2</span>) {\n        <span class=\"fn\">fprintf</span>(<span class=\"pp\">stderr</span>, <span class=\"str\">\"Usage: %s &lt;name&gt;\\n\"</span>, argv[<span class=\"num\">0</span>]);\n        <span class=\"kw\">return</span> <span class=\"num\">1</span>;    <span class=\"cm\">/* sıfır olmayan = hata */</span>\n    }\n    <span class=\"fn\">printf</span>(<span class=\"str\">\"Hello, %s! Welcome to C.\\n\"</span>, argv[<span class=\"num\">1</span>]);\n    <span class=\"kw\">return</span> <span class=\"num\">0</span>;\n}</code></pre></div></div><div class=\"code-wrap\"><div class=\"code-label\"><span>BASH</span> Derle &amp; Çalıştır <button class=\"code-copy\">COPY</button></div><div class=\"code-block\"><pre><code>gcc -Wall -Wextra -std=c11 greet.c -o greet\n./greet Mikail\n./greet</code></pre></div></div><div class=\"code-wrap\"><div class=\"code-label\"><span>OUTPUT</span></div><div class=\"code-block output-block\"><pre><code>Hello, Mikail! Welcome to C.\nUsage: ./greet &lt;name&gt;</code></pre></div></div><p class=\"l-text\">Dikkat edin: hata mesajları için <code>printf</code> yerine <code>fprintf(stderr, ...)</code> kullanıyoruz. Bu, normal çıktıdan ayrı olarak yönlendirilebilen hata akışına yazar. <code>1</code> dönüş değeri işletim sistemine (ve kabuk betiklerine) bir şeylerin yanlış gittiğini söyler.</p><div class=\"l-warn\"><strong>Yaygın başlangıç hataları:</strong></div><ul class=\"l-list\"><li><strong><code>#include &lt;stdio.h&gt;</code> unutmak</strong> — \"implicit declaration of function printf\" uyarısı alırsınız</li><li><strong>Eksik noktalı virgüller</strong> — C her ifadenin sonunda <code>;</code> gerektirir. Hata genellikle noktalı virgülün eksik olduğu satıra değil, bir sonraki satıra işaret eder.</li><li><strong><code>==</code> yerine <code>=</code> kullanmak</strong> — <code>=</code> atama, <code>==</code> karşılaştırmadır. <code>if (x = 5)</code> x'e 5 atar ve her zaman doğru olarak değerlendirilir.</li><li><strong>Derleyici uyarılarını kontrol etmemek</strong> — <code>-Wall</code> olmadan derlerseniz, derleyici potansiyel hataları sessizce görmezden gelir</li><li><strong>Dosyanızı <code>test.c</code> olarak adlandırmak</strong> — birçok sistemde <code>test</code> yerleşik bir komuttur. <code>mytest.c</code> gibi özel bir isim verin.</li></ul>"
};
