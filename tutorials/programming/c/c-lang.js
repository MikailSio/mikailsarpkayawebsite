/* c.js — C Language: all translatable content */
(function(){
'use strict';

const L = {
en: {
  eyebrow: 'Tutorial 01 \u00b7 Systems Programming',
  heroDesc: 'The foundation of systems programming. C gives you direct control over memory and hardware. Understanding C makes you a <strong>better programmer</strong> \u2014 because it shows you what\u2019s really happening underneath.',
  metaLessons: 'Lessons', metaLevel: 'Level', metaCompiler: 'Compiler',
  prevLabel: '\u2190 All Tutorials', nextLabel: 'Next: Python \u2192', badge: 'Available',
  lessons: [
    {
      title: 'Hello World &amp; Compilation',
      sub: 'main(), gcc, clang, compilation pipeline, header files',
      time: '5 min read',
      body: '<p class="l-text">Every C program starts with <code>main()</code>. Before your code runs, the compiler takes your <code>.c</code> source file through four distinct stages: preprocessing, compilation, assembling, and linking.</p><div class="code-wrap"><div class="code-label"><span>C</span> hello.c <button class="code-copy">COPY</button></div><div class="code-block"><pre><code><span class="pp">#include</span> <span class="str">&lt;stdio.h&gt;</span>    <span class="cm">/* standard I/O header */</span>\n\n<span class="ty">int</span> <span class="fn">main</span>(<span class="ty">void</span>) {\n    <span class="fn">printf</span>(<span class="str">"Hello, World!\\n"</span>);\n    <span class="kw">return</span> <span class="num">0</span>;\n}</code></pre></div></div><div class="code-wrap"><div class="code-label"><span>BASH</span> Compile &amp; Run <button class="code-copy">COPY</button></div><div class="code-block"><pre><code><span class="cm"># Basic compile</span>\ngcc hello.c -o hello\n./hello\n\n<span class="cm"># Recommended</span>\ngcc -Wall -Wextra -std=c11 hello.c -o hello\n\n<span class="cm"># Debug symbols</span>\ngcc -g -Wall hello.c -o hello</code></pre></div></div><p class="l-text">The four <strong>compilation stages</strong>:</p><ul class="l-list"><li><strong>Preprocessing</strong> \u2014 expands <code>#include</code>, <code>#define</code> macros (.i file)</li><li><strong>Compilation</strong> \u2014 C to assembly (.s file)</li><li><strong>Assembling</strong> \u2014 assembly to machine code (.o)</li><li><strong>Linking</strong> \u2014 combines .o files + libraries into executable</li></ul><div class="l-note"><strong>Tip:</strong> Always compile with <code>-Wall -Wextra</code>. Warnings are bugs waiting to happen.</div><div class="code-wrap"><div class="code-label"><span>C</span> Command-line arguments <button class="code-copy">COPY</button></div><div class="code-block"><pre><code><span class="pp">#include</span> <span class="str">&lt;stdio.h&gt;</span>\n\n<span class="ty">int</span> <span class="fn">main</span>(<span class="ty">int</span> argc, <span class="ty">char</span> *argv[]) {\n    <span class="fn">printf</span>(<span class="str">"Arguments: %d\\n"</span>, argc);\n    <span class="kw">for</span> (<span class="ty">int</span> i = <span class="num">0</span>; i &lt; argc; i++)\n        <span class="fn">printf</span>(<span class="str">"  argv[%d] = %s\\n"</span>, i, argv[i]);\n    <span class="kw">return</span> <span class="num">0</span>;\n}</code></pre></div></div>'
    },
    {
      title: 'Variables &amp; Data Types',
      sub: 'int, float, double, char, sizeof, limits, type casting',
      time: '8 min read',
      body: '<p class="l-text">C is a <strong>statically typed</strong> language \u2014 every variable must be declared with a type before use.</p><div class="code-wrap"><div class="code-label"><span>C</span> Primitive types <button class="code-copy">COPY</button></div><div class="code-block"><pre><code><span class="pp">#include</span> <span class="str">&lt;stdio.h&gt;</span>\n\n<span class="ty">int</span> <span class="fn">main</span>(<span class="ty">void</span>) {\n    <span class="ty">char</span>      c  = <span class="str">\'A\'</span>;              <span class="cm">/* 1 byte */</span>\n    <span class="ty">short</span>     s  = <span class="num">32000</span>;            <span class="cm">/* 2 bytes */</span>\n    <span class="ty">int</span>       i  = <span class="num">2147483647</span>;       <span class="cm">/* 4 bytes */</span>\n    <span class="ty">long</span>      l  = <span class="num">9999999L</span>;         <span class="cm">/* 8 bytes */</span>\n    <span class="ty">long long</span> ll = <span class="num">9223372036854775807LL</span>;\n\n    <span class="ty">float</span>  f = <span class="num">3.14f</span>;              <span class="cm">/* ~7 digits */</span>\n    <span class="ty">double</span> d = <span class="num">3.14159265358</span>;      <span class="cm">/* ~15 digits */</span>\n    <span class="ty">unsigned int</span> u = <span class="num">4294967295U</span>;\n\n    <span class="fn">printf</span>(<span class="str">"int=%zu double=%zu char=%zu\\n"</span>,\n           <span class="kw">sizeof</span>(<span class="ty">int</span>), <span class="kw">sizeof</span>(<span class="ty">double</span>), <span class="kw">sizeof</span>(<span class="ty">char</span>));\n    <span class="kw">return</span> <span class="num">0</span>;\n}</code></pre></div></div><div class="code-wrap"><div class="code-label"><span>C</span> Type casting <button class="code-copy">COPY</button></div><div class="code-block"><pre><code><span class="ty">int</span> a = <span class="num">7</span>, b = <span class="num">2</span>;\n<span class="fn">printf</span>(<span class="str">"%d\\n"</span>, a / b);           <span class="cm">/* 3, not 3.5 */</span>\n<span class="fn">printf</span>(<span class="str">"%.2f\\n"</span>, (<span class="ty">double</span>)a / b); <span class="cm">/* 3.50 */</span>\n\n<span class="ty">double</span> pi = <span class="num">3.99</span>;\n<span class="ty">int</span> n = (<span class="ty">int</span>)pi;   <span class="cm">/* n = 3 */</span></code></pre></div></div><ul class="l-list"><li><strong>Prefer double over float</strong> \u2014 float has only ~7 decimal digits</li><li><strong>Use unsigned</strong> when value can never be negative</li><li><strong>sizeof returns size_t</strong> \u2014 use <code>%zu</code></li></ul><div class="code-wrap"><div class="code-label"><span>C</span> Fixed-width types <button class="code-copy">COPY</button></div><div class="code-block"><pre><code><span class="pp">#include</span> <span class="str">&lt;stdint.h&gt;</span>\n\n<span class="ty">int8_t</span>   a = <span class="num">127</span>;          <span class="cm">/* 8 bits */</span>\n<span class="ty">int32_t</span>  c = <span class="num">2147483647</span>;   <span class="cm">/* 32 bits */</span>\n<span class="ty">uint32_t</span> e = <span class="num">4294967295U</span>;  <span class="cm">/* unsigned 32 */</span></code></pre></div></div><div class="l-warn"><strong>Rule:</strong> Use <code>int32_t</code>, <code>uint64_t</code> from <code>&lt;stdint.h&gt;</code> in production. <code>int</code> size is platform-dependent.</div>'
    },
    {
      title: 'Pointers &amp; Memory',
      sub: '*, &amp;, malloc, calloc, realloc, free, NULL, memory leaks',
      time: '12 min read',
      body: '<p class="l-text">A pointer stores a <strong>memory address</strong>. This is the most powerful \u2014 and dangerous \u2014 feature of C.</p><div class="code-wrap"><div class="code-label"><span>C</span> Pointer basics <button class="code-copy">COPY</button></div><div class="code-block"><pre><code><span class="pp">#include</span> <span class="str">&lt;stdio.h&gt;</span>\n\n<span class="ty">int</span> <span class="fn">main</span>(<span class="ty">void</span>) {\n    <span class="ty">int</span> x = <span class="num">42</span>;\n    <span class="ty">int</span> *ptr = &amp;x;\n    <span class="fn">printf</span>(<span class="str">"Value: %d\\n"</span>, x);\n    <span class="fn">printf</span>(<span class="str">"Addr:  %p\\n"</span>, ptr);\n    <span class="fn">printf</span>(<span class="str">"Deref: %d\\n"</span>, *ptr);\n    *ptr = <span class="num">100</span>;\n    <span class="fn">printf</span>(<span class="str">"x now: %d\\n"</span>, x); <span class="cm">/* 100 */</span>\n    <span class="kw">return</span> <span class="num">0</span>;\n}</code></pre></div></div><div class="code-wrap"><div class="code-label"><span>C</span> Swap via pointers <button class="code-copy">COPY</button></div><div class="code-block"><pre><code><span class="ty">void</span> <span class="fn">swap</span>(<span class="ty">int</span> *a, <span class="ty">int</span> *b) {\n    <span class="ty">int</span> tmp = *a; *a = *b; *b = tmp;\n}\n<span class="ty">int</span> x=<span class="num">5</span>, y=<span class="num">10</span>;\n<span class="fn">swap</span>(&amp;x, &amp;y);\n<span class="fn">printf</span>(<span class="str">"%d %d\\n"</span>, x, y); <span class="cm">/* 10 5 */</span></code></pre></div></div><div class="code-wrap"><div class="code-label"><span>C</span> Dynamic memory <button class="code-copy">COPY</button></div><div class="code-block"><pre><code><span class="pp">#include</span> <span class="str">&lt;stdlib.h&gt;</span>\n\n<span class="ty">int</span> *arr = (<span class="ty">int</span>*)<span class="fn">malloc</span>(<span class="num">5</span> * <span class="kw">sizeof</span>(<span class="ty">int</span>));\n<span class="kw">if</span> (!arr) { <span class="fn">perror</span>(<span class="str">"malloc"</span>); <span class="kw">return</span> <span class="num">1</span>; }\n\n<span class="ty">int</span> *z = (<span class="ty">int</span>*)<span class="fn">calloc</span>(<span class="num">5</span>, <span class="kw">sizeof</span>(<span class="ty">int</span>));\narr = (<span class="ty">int</span>*)<span class="fn">realloc</span>(arr, <span class="num">10</span>*<span class="kw">sizeof</span>(<span class="ty">int</span>));\n<span class="fn">free</span>(arr); <span class="fn">free</span>(z);\narr = <span class="pp">NULL</span>;</code></pre></div></div><div class="l-warn"><strong>Common bugs:</strong> forgetting free (leak), use-after-free (UB), double free (crash). Use <code>valgrind ./prog</code>.</div><div class="code-wrap"><div class="code-label"><span>C</span> Pointer arithmetic <button class="code-copy">COPY</button></div><div class="code-block"><pre><code><span class="ty">int</span> arr[] = {<span class="num">10</span>,<span class="num">20</span>,<span class="num">30</span>,<span class="num">40</span>,<span class="num">50</span>};\n<span class="ty">int</span> *p = arr;\n<span class="fn">printf</span>(<span class="str">"%d\\n"</span>, *p);       <span class="cm">/* 10 */</span>\n<span class="fn">printf</span>(<span class="str">"%d\\n"</span>, *(p+<span class="num">2</span>));   <span class="cm">/* 30 */</span>\n<span class="fn">printf</span>(<span class="str">"%d\\n"</span>, p[<span class="num">3</span>]);     <span class="cm">/* 40 */</span></code></pre></div></div>'
    },
    {
      title: 'Functions &amp; Recursion',
      sub: 'prototypes, pass-by-value, call stack, recursion, function pointers',
      time: '10 min read',
      body: '<p class="l-text">C is <strong>pass-by-value</strong> \u2014 functions receive copies. Pass a pointer to modify caller\u2019s variables.</p><div class="code-wrap"><div class="code-label"><span>C</span> Function prototype <button class="code-copy">COPY</button></div><div class="code-block"><pre><code><span class="pp">#include</span> <span class="str">&lt;stdio.h&gt;</span>\n\n<span class="ty">double</span> <span class="fn">power</span>(<span class="ty">double</span> base, <span class="ty">int</span> exp);\n\n<span class="ty">int</span> <span class="fn">main</span>(<span class="ty">void</span>) {\n    <span class="fn">printf</span>(<span class="str">"2^10 = %.0f\\n"</span>, <span class="fn">power</span>(<span class="num">2.0</span>, <span class="num">10</span>));\n    <span class="kw">return</span> <span class="num">0</span>;\n}\n\n<span class="ty">double</span> <span class="fn">power</span>(<span class="ty">double</span> base, <span class="ty">int</span> exp) {\n    <span class="ty">double</span> r = <span class="num">1.0</span>;\n    <span class="kw">for</span>(<span class="ty">int</span> i=<span class="num">0</span>; i&lt;exp; i++) r *= base;\n    <span class="kw">return</span> r;\n}</code></pre></div></div><div class="code-wrap"><div class="code-label"><span>C</span> Recursion \u2014 factorial <button class="code-copy">COPY</button></div><div class="code-block"><pre><code><span class="ty">long long</span> <span class="fn">factorial</span>(<span class="ty">int</span> n) {\n    <span class="kw">if</span> (n &lt;= <span class="num">1</span>) <span class="kw">return</span> <span class="num">1</span>;\n    <span class="kw">return</span> n * <span class="fn">factorial</span>(n-<span class="num">1</span>);\n}\n<span class="fn">printf</span>(<span class="str">"10! = %lld\\n"</span>, <span class="fn">factorial</span>(<span class="num">10</span>));</code></pre></div></div><div class="code-wrap"><div class="code-label"><span>C</span> Function pointers <button class="code-copy">COPY</button></div><div class="code-block"><pre><code><span class="ty">int</span> <span class="fn">cmp_int</span>(<span class="kw">const</span> <span class="ty">void</span> *a, <span class="kw">const</span> <span class="ty">void</span> *b) {\n    <span class="kw">return</span> *(<span class="kw">const</span> <span class="ty">int</span>*)a - *(<span class="kw">const</span> <span class="ty">int</span>*)b;\n}\n<span class="ty">int</span> d[] = {<span class="num">5</span>,<span class="num">1</span>,<span class="num">4</span>,<span class="num">2</span>,<span class="num">8</span>};\n<span class="fn">qsort</span>(d, <span class="num">5</span>, <span class="kw">sizeof</span>(<span class="ty">int</span>), cmp_int);</code></pre></div></div><div class="l-warn"><strong>Stack depth:</strong> Deep recursion can overflow. Convert to iteration for large inputs.</div>'
    },
    {
      title: 'Arrays &amp; Strings',
      sub: 'stack arrays, char arrays, null terminator, string.h',
      time: '10 min read',
      body: '<p class="l-text">An array is a contiguous block of memory. Strings are <strong>char arrays</strong> ending with <code>\'\\0\'</code>.</p><div class="code-wrap"><div class="code-label"><span>C</span> Arrays <button class="code-copy">COPY</button></div><div class="code-block"><pre><code><span class="pp">#include</span> <span class="str">&lt;stdio.h&gt;</span>\n\n<span class="ty">int</span> scores[] = {<span class="num">85</span>,<span class="num">92</span>,<span class="num">78</span>,<span class="num">95</span>,<span class="num">88</span>};\n<span class="ty">int</span> len = <span class="kw">sizeof</span>(scores)/<span class="kw">sizeof</span>(scores[<span class="num">0</span>]);\n<span class="ty">int</span> sum = <span class="num">0</span>;\n<span class="kw">for</span>(<span class="ty">int</span> i=<span class="num">0</span>; i&lt;len; i++) sum += scores[i];\n<span class="fn">printf</span>(<span class="str">"Avg: %.1f\\n"</span>, (<span class="ty">double</span>)sum/len);\n\n<span class="ty">int</span> mat[<span class="num">3</span>][<span class="num">3</span>] = {{<span class="num">1</span>,<span class="num">2</span>,<span class="num">3</span>},{<span class="num">4</span>,<span class="num">5</span>,<span class="num">6</span>},{<span class="num">7</span>,<span class="num">8</span>,<span class="num">9</span>}};\n<span class="fn">printf</span>(<span class="str">"mat[1][2]=%d\\n"</span>, mat[<span class="num">1</span>][<span class="num">2</span>]); <span class="cm">/* 6 */</span></code></pre></div></div><div class="code-wrap"><div class="code-label"><span>C</span> Strings <button class="code-copy">COPY</button></div><div class="code-block"><pre><code><span class="pp">#include</span> <span class="str">&lt;string.h&gt;</span>\n\n<span class="ty">char</span> name[] = <span class="str">"Mikail"</span>;\n<span class="fn">printf</span>(<span class="str">"Len: %zu\\n"</span>, <span class="fn">strlen</span>(name)); <span class="cm">/* 6 */</span>\n\n<span class="ty">char</span> buf[<span class="num">50</span>];\n<span class="fn">strncpy</span>(buf, name, <span class="kw">sizeof</span>(buf)-<span class="num">1</span>);\nbuf[<span class="kw">sizeof</span>(buf)-<span class="num">1</span>] = <span class="str">\'\\0\'</span>;\n<span class="fn">strncat</span>(buf, <span class="str">" Sarpkaya"</span>, <span class="kw">sizeof</span>(buf)-<span class="fn">strlen</span>(buf)-<span class="num">1</span>);\n\n<span class="kw">if</span>(<span class="fn">strcmp</span>(name,<span class="str">"Mikail"</span>)==<span class="num">0</span>)\n    <span class="fn">printf</span>(<span class="str">"Match!\\n"</span>);</code></pre></div></div><div class="l-warn"><strong>Never use:</strong> <code>strcpy</code>, <code>strcat</code>, <code>gets</code> \u2014 buffer overflow risk. Use <code>strncpy</code>, <code>strncat</code>, <code>fgets</code>.</div>'
    },
    {
      title: 'File I/O in C',
      sub: 'fopen, fclose, fprintf, fgets, fread, fwrite, binary files',
      time: '8 min read',
      body: '<p class="l-text">File I/O uses <strong>FILE pointers</strong>. Always check <code>fopen</code> and always call <code>fclose</code>.</p><div class="code-wrap"><div class="code-label"><span>C</span> Text file I/O <button class="code-copy">COPY</button></div><div class="code-block"><pre><code><span class="pp">#include</span> <span class="str">&lt;stdio.h&gt;</span>\n\n<span class="ty">int</span> <span class="fn">main</span>(<span class="ty">void</span>) {\n    <span class="ty">FILE</span> *fw = <span class="fn">fopen</span>(<span class="str">"data.txt"</span>, <span class="str">"w"</span>);\n    <span class="kw">if</span>(!fw) { <span class="fn">perror</span>(<span class="str">"fopen"</span>); <span class="kw">return</span> <span class="num">1</span>; }\n    <span class="fn">fprintf</span>(fw, <span class="str">"Name: %s\\n"</span>, <span class="str">"Mikail"</span>);\n    <span class="fn">fclose</span>(fw);\n\n    <span class="ty">FILE</span> *fr = <span class="fn">fopen</span>(<span class="str">"data.txt"</span>, <span class="str">"r"</span>);\n    <span class="ty">char</span> line[<span class="num">256</span>];\n    <span class="kw">while</span>(<span class="fn">fgets</span>(line, <span class="kw">sizeof</span>(line), fr))\n        <span class="fn">printf</span>(<span class="str">"Read: %s"</span>, line);\n    <span class="fn">fclose</span>(fr);\n    <span class="kw">return</span> <span class="num">0</span>;\n}</code></pre></div></div><div class="code-wrap"><div class="code-label"><span>C</span> Binary struct <button class="code-copy">COPY</button></div><div class="code-block"><pre><code><span class="kw">typedef struct</span> {\n    <span class="ty">char</span> name[<span class="num">32</span>]; <span class="ty">float</span> score; <span class="ty">int</span> year;\n} <span class="ty">Record</span>;\n\n<span class="ty">Record</span> r = {<span class="str">"Mikail"</span>, <span class="num">95.5f</span>, <span class="num">2025</span>};\n<span class="ty">FILE</span> *fb = <span class="fn">fopen</span>(<span class="str">"rec.bin"</span>, <span class="str">"wb"</span>);\n<span class="fn">fwrite</span>(&amp;r, <span class="kw">sizeof</span>(<span class="ty">Record</span>), <span class="num">1</span>, fb);\n<span class="fn">fclose</span>(fb);</code></pre></div></div><ul class="l-list"><li><code>"r"</code> read, <code>"w"</code> write, <code>"a"</code> append</li><li><code>"rb"</code>/<code>"wb"</code> for binary</li><li><code>fseek</code>+<code>ftell</code> to navigate</li></ul><div class="l-note"><strong>Always:</strong> check <code>fopen</code>, close with <code>fclose</code>, use <code>fgets</code> not <code>scanf</code> for strings.</div>'
    }
  ]
},
tr: {
  eyebrow: 'E\u011fitim 01 \u00b7 Sistem Programlama',
  heroDesc: 'Sistem programlaman\u0131n temeli. C, bellek ve donan\u0131m \u00fczerinde do\u011frudan kontrol sa\u011flar. C\u2019yi anlamak sizi <strong>daha iyi bir programc\u0131</strong> yapar \u2014 \u00e7\u00fcnk\u00fc altta yatan ger\u00e7ek s\u00fcre\u00e7leri g\u00f6sterir.',
  metaLessons: 'Dersler', metaLevel: 'Seviye', metaCompiler: 'Derleyici',
  prevLabel: '\u2190 T\u00fcm E\u011fitimler', nextLabel: 'Sonraki: Python \u2192', badge: 'Mevcut',
  lessons: [
    {title:'Merhaba D\u00fcnya &amp; Derleme',sub:'main(), gcc, clang, derleme, ba\u015fl\u0131k dosyalar\u0131',time:'5 dk okuma',body:null},
    {title:'De\u011fi\u015fkenler &amp; Veri Tipleri',sub:'int, float, double, char, sizeof, limitler',time:'8 dk okuma',body:null},
    {title:'\u0130\u015faret\u00e7iler &amp; Bellek',sub:'*, &amp;, malloc, calloc, realloc, free, NULL',time:'12 dk okuma',body:null},
    {title:'Fonksiyonlar &amp; \u00d6zyineleme',sub:'prototipler, de\u011fer ile ge\u00e7i\u015f, \u00f6zyineleme',time:'10 dk okuma',body:null},
    {title:'Diziler &amp; Dizgiler',sub:'char dizileri, null sonland\u0131r\u0131c\u0131, string.h',time:'10 dk okuma',body:null},
    {title:'C\'de Dosya \u0130\u015flemleri',sub:'fopen, fclose, fprintf, fgets, fread, fwrite',time:'8 dk okuma',body:null}
  ]
}
};

function render(lang) {
  var t = L[lang] || L.en, fb = L.en;
  var ey = document.getElementById('eyebrow');
  if(ey) ey.textContent = t.eyebrow;
  var hd = document.getElementById('heroDesc');
  if(hd) hd.innerHTML = t.heroDesc;
  document.querySelectorAll('.meta-label').forEach(function(el){
    var k=el.dataset.key; if(k&&t[k]) el.textContent=t[k];
  });
  var pv=document.getElementById('prevLabel');
  if(pv) pv.textContent=t.prevLabel;
  var nx=document.getElementById('nextLabel');
  if(nx) nx.textContent=t.nextLabel;
  document.querySelectorAll('.lesson-block').forEach(function(block,i){
    var lesson=t.lessons[i], fall=fb.lessons[i];
    if(!lesson) return;
    var ti=block.querySelector('.lesson-title');
    var su=block.querySelector('.lesson-sub');
    var tm=block.querySelector('.l-time');
    var ba=block.querySelector('.l-badge-text');
    var bo=block.querySelector('.lesson-body');
    if(ti) ti.innerHTML=lesson.title;
    if(su) su.innerHTML=lesson.sub;
    if(tm) tm.textContent=lesson.time;
    if(ba) ba.textContent=t.badge;
    var content=lesson.body||(fall?fall.body:'');
    if(bo&&content) bo.innerHTML=content;
  });
}
render('en');
document.addEventListener('langchange',function(e){render(e.detail.lang);});
try{if(localStorage.getItem('tut-lang')==='tr') render('tr');}catch(e){}
})();
