var PYTHON_L7 = {

en: '<p class="l-text"><strong>Modern Python is not the Python of 2015.</strong> The language has evolved aggressively: <code>dataclasses</code> killed boilerplate, <code>match/case</code> brought structural pattern matching, <code>pathlib</code> retired <code>os.path</code>, type hints became table stakes, and tooling shifted from <code>pip</code>+<code>venv</code> to <code>uv</code> overnight. If you write Python like it is 2018, you are writing legacy code on day one.</p>'

+ '<p class="l-text">This lesson is a tour of the features and tools that define <em>idiomatic 2026 Python</em>. Every section is something you should reach for daily, not occasionally. Skip these and your code will look ten years older than it is — verbose, fragile, and hard to refactor.</p>'

+ '<div class="lesson-outcomes" style="background:rgba(200,169,110,0.06);border-left:3px solid #c8a96e;padding:1rem 1.2rem;margin:1.5rem 0;border-radius:0 8px 8px 0"><div style="font-size:0.78rem;font-weight:700;letter-spacing:0.08em;color:#c8a96e;margin-bottom:0.6rem">📍 WHAT YOU\'LL LEARN</div><ul style="margin:0;padding-left:1.3rem;line-height:1.65;font-size:0.93rem;color:rgba(235,230,220,0.92)"><li>Replace boilerplate <code>__init__</code> classes with <code>@dataclass</code> in one line</li><li>Refactor nested <code>if/elif</code> chains into <code>match/case</code> structural patterns</li><li>Write modern type hints with <code>X | Y</code>, <code>list[int]</code>, <code>Protocol</code>, <code>TypeVar</code></li><li>Stop using <code>os.path</code> — manipulate filesystems with <code>pathlib.Path</code></li><li>Use the walrus operator, advanced f-strings, and <code>__slots__</code> for memory savings</li><li>Manage projects with <code>pyproject.toml</code> and <code>uv</code> (the new standard)</li></ul></div>'


/* ============================================================
   SECTION 1: DATACLASSES
   ============================================================ */
+ '<h2 class="l-title">1. Dataclasses — Goodbye Boilerplate</h2>'

+ '<div class="calc-highlight"><strong>The problem:</strong> Writing a class that mostly holds data means typing the same fields three times — once in <code>__init__</code>, once as attributes, once in <code>__repr__</code>. Dataclasses generate all of that automatically from a single declaration.</div>'

+ '<p class="l-text"><strong>Before vs After</strong> — Watch fifteen lines collapse into four:</p>'

+ '<div class="code-wrap"><div class="code-label"><span>PYTHON</span><button class="copy-btn" onclick="navigator.clipboard.writeText(this.closest(\'.code-wrap\').querySelector(\'code\').textContent)">COPY</button></div><pre class="code-block"><code><span class="cm"># BEFORE — manual class, every line written by hand</span>\n<span class="kw">class</span> <span class="fn">Point</span>:\n    <span class="kw">def</span> <span class="fn">__init__</span>(<span class="kw">self</span>, x, y, label=<span class="str">""</span>):\n        <span class="kw">self</span>.x = x\n        <span class="kw">self</span>.y = y\n        <span class="kw">self</span>.label = label\n\n    <span class="kw">def</span> <span class="fn">__repr__</span>(<span class="kw">self</span>):\n        <span class="kw">return</span> <span class="fn">f</span><span class="str">"Point(x={self.x}, y={self.y}, label={self.label!r})"</span>\n\n    <span class="kw">def</span> <span class="fn">__eq__</span>(<span class="kw">self</span>, other):\n        <span class="kw">return</span> (<span class="kw">self</span>.x, <span class="kw">self</span>.y, <span class="kw">self</span>.label) == (other.x, other.y, other.label)\n\n<span class="cm"># AFTER — same behavior, one decorator</span>\n<span class="kw">from</span> dataclasses <span class="kw">import</span> dataclass\n\n<span class="kw">@dataclass</span>\n<span class="kw">class</span> <span class="fn">Point</span>:\n    x: <span class="ty">float</span>\n    y: <span class="ty">float</span>\n    label: <span class="ty">str</span> = <span class="str">""</span>\n\np = <span class="fn">Point</span>(<span class="num">3</span>, <span class="num">4</span>, <span class="str">"origin"</span>)\n<span class="fn">print</span>(p)            <span class="cm"># Point(x=3, y=4, label=\'origin\')</span>\n<span class="fn">print</span>(p == <span class="fn">Point</span>(<span class="num">3</span>, <span class="num">4</span>, <span class="str">"origin"</span>))  <span class="cm"># True — auto __eq__</span></code></pre></div>'

+ '<p class="l-text">The <code>@dataclass</code> decorator inspects the class annotations and writes <code>__init__</code>, <code>__repr__</code>, and <code>__eq__</code> for you. The fields are real attributes — you can subclass, add methods, override anything.</p>'

+ '<p class="l-text"><strong>Mutable defaults — the <code>field</code> trap</strong> — Never write <code>tags: list = []</code> as a default. All instances would share the same list. Use <code>field(default_factory=list)</code> instead, which creates a fresh list per instance:</p>'

+ '<div class="code-wrap"><div class="code-label"><span>PYTHON</span><button class="copy-btn" onclick="navigator.clipboard.writeText(this.closest(\'.code-wrap\').querySelector(\'code\').textContent)">COPY</button></div><pre class="code-block"><code><span class="kw">from</span> dataclasses <span class="kw">import</span> dataclass, field\n\n<span class="kw">@dataclass</span>\n<span class="kw">class</span> <span class="fn">Document</span>:\n    title: <span class="ty">str</span>\n    tags: <span class="ty">list</span>[<span class="ty">str</span>] = <span class="fn">field</span>(default_factory=<span class="ty">list</span>)\n    metadata: <span class="ty">dict</span>[<span class="ty">str</span>, <span class="ty">str</span>] = <span class="fn">field</span>(default_factory=<span class="ty">dict</span>)\n    word_count: <span class="ty">int</span> = <span class="num">0</span>\n\nd1 = <span class="fn">Document</span>(<span class="str">"NLP Basics"</span>)\nd1.tags.append(<span class="str">"intro"</span>)\n\nd2 = <span class="fn">Document</span>(<span class="str">"Transformers"</span>)\n<span class="fn">print</span>(d2.tags)   <span class="cm"># [] — NOT polluted by d1, fresh list per instance</span></code></pre></div>'

+ '<p class="l-text"><strong>Frozen dataclasses</strong> — Add <code>frozen=True</code> and the instance becomes immutable. This gives you <code>__hash__</code> for free, so the object can live in sets and dict keys — perfect for value objects, config snapshots, and cache keys:</p>'

+ '<div class="code-wrap"><div class="code-label"><span>PYTHON</span><button class="copy-btn" onclick="navigator.clipboard.writeText(this.closest(\'.code-wrap\').querySelector(\'code\').textContent)">COPY</button></div><pre class="code-block"><code><span class="kw">from</span> dataclasses <span class="kw">import</span> dataclass\n\n<span class="kw">@dataclass</span>(frozen=<span class="kw">True</span>, slots=<span class="kw">True</span>)\n<span class="kw">class</span> <span class="fn">ModelConfig</span>:\n    name: <span class="ty">str</span>\n    hidden_dim: <span class="ty">int</span>\n    n_layers: <span class="ty">int</span>\n    dropout: <span class="ty">float</span> = <span class="num">0.1</span>\n\ncfg = <span class="fn">ModelConfig</span>(<span class="str">"transformer-base"</span>, <span class="num">512</span>, <span class="num">6</span>)\n<span class="cm"># cfg.dropout = 0.2  # FrozenInstanceError — cannot mutate</span>\n\n<span class="cm"># Hashable → usable as dict key</span>\nresults = {cfg: <span class="num">0.87</span>}\n<span class="fn">print</span>(results[<span class="fn">ModelConfig</span>(<span class="str">"transformer-base"</span>, <span class="num">512</span>, <span class="num">6</span>)])  <span class="cm"># 0.87</span></code></pre></div>'

+ '<p class="l-text"><strong><code>__post_init__</code></strong> — Need to validate or derive a field after the auto-generated <code>__init__</code> runs? Define <code>__post_init__</code> and the dataclass will call it:</p>'

+ '<div class="code-wrap"><div class="code-label"><span>PYTHON</span><button class="copy-btn" onclick="navigator.clipboard.writeText(this.closest(\'.code-wrap\').querySelector(\'code\').textContent)">COPY</button></div><pre class="code-block"><code><span class="kw">from</span> dataclasses <span class="kw">import</span> dataclass, field\n\n<span class="kw">@dataclass</span>\n<span class="kw">class</span> <span class="fn">Tensor</span>:\n    shape: <span class="ty">tuple</span>[<span class="ty">int</span>, ...]\n    dtype: <span class="ty">str</span> = <span class="str">"float32"</span>\n    n_elements: <span class="ty">int</span> = <span class="fn">field</span>(init=<span class="kw">False</span>)  <span class="cm"># derived, not an arg</span>\n\n    <span class="kw">def</span> <span class="fn">__post_init__</span>(<span class="kw">self</span>):\n        <span class="kw">if</span> <span class="fn">any</span>(d <= <span class="num">0</span> <span class="kw">for</span> d <span class="kw">in</span> <span class="kw">self</span>.shape):\n            <span class="kw">raise</span> <span class="fn">ValueError</span>(<span class="fn">f</span><span class="str">"Invalid shape: {self.shape}"</span>)\n        <span class="kw">import</span> math\n        <span class="kw">self</span>.n_elements = math.prod(<span class="kw">self</span>.shape)\n\nt = <span class="fn">Tensor</span>(shape=(<span class="num">32</span>, <span class="num">128</span>, <span class="num">768</span>))\n<span class="fn">print</span>(t.n_elements)  <span class="cm"># 3145728</span></code></pre></div>'

+ '<div class="l-note"><strong>Reach for dataclass when:</strong> the class is primarily a record of fields. Reach for a regular <code>class</code> when behavior dominates and fields are incidental. For runtime-validated schemas (API payloads, user input), use <code>pydantic.BaseModel</code> instead — it builds on dataclass ideas and adds type coercion.</div>'


/* ============================================================
   SECTION 2: PATTERN MATCHING (match/case)
   ============================================================ */
+ '<h2 class="l-title">2. Pattern Matching — match/case</h2>'

+ '<div class="calc-highlight">Introduced in Python 3.10, <code>match/case</code> is <em>structural</em> pattern matching: it does not just compare equality like a <code>switch</code> — it destructures objects, sequences, and mappings, optionally binding parts of them to names. Once you internalize it, half of your nested <code>if/elif</code> chains evaporate.</div>'

+ '<p class="l-text"><strong>Literal and capture patterns</strong> — The simplest form is a <code>switch</code> replacement. The wildcard <code>_</code> catches anything not matched above:</p>'

+ '<div class="code-wrap"><div class="code-label"><span>PYTHON</span><button class="copy-btn" onclick="navigator.clipboard.writeText(this.closest(\'.code-wrap\').querySelector(\'code\').textContent)">COPY</button></div><pre class="code-block"><code><span class="kw">def</span> <span class="fn">http_status</span>(code: <span class="ty">int</span>) -> <span class="ty">str</span>:\n    <span class="kw">match</span> code:\n        <span class="kw">case</span> <span class="num">200</span> | <span class="num">201</span> | <span class="num">204</span>:\n            <span class="kw">return</span> <span class="str">"Success"</span>\n        <span class="kw">case</span> <span class="num">301</span> | <span class="num">302</span>:\n            <span class="kw">return</span> <span class="str">"Redirect"</span>\n        <span class="kw">case</span> <span class="num">404</span>:\n            <span class="kw">return</span> <span class="str">"Not found"</span>\n        <span class="kw">case</span> n <span class="kw">if</span> <span class="num">500</span> <= n < <span class="num">600</span>:        <span class="cm"># guard clause</span>\n            <span class="kw">return</span> <span class="fn">f</span><span class="str">"Server error ({n})"</span>\n        <span class="kw">case</span> _:\n            <span class="kw">return</span> <span class="str">"Unknown"</span>\n\n<span class="fn">print</span>(<span class="fn">http_status</span>(<span class="num">503</span>))  <span class="cm"># "Server error (503)"</span></code></pre></div>'

+ '<p class="l-text"><strong>Sequence patterns</strong> — Patterns can destructure lists and tuples, including the rest with <code>*name</code>. This is where <code>match</code> outshines <code>if</code>:</p>'

+ '<div class="code-wrap"><div class="code-label"><span>PYTHON</span><button class="copy-btn" onclick="navigator.clipboard.writeText(this.closest(\'.code-wrap\').querySelector(\'code\').textContent)">COPY</button></div><pre class="code-block"><code><span class="kw">def</span> <span class="fn">describe</span>(items):\n    <span class="kw">match</span> items:\n        <span class="kw">case</span> []:\n            <span class="kw">return</span> <span class="str">"empty"</span>\n        <span class="kw">case</span> [single]:\n            <span class="kw">return</span> <span class="fn">f</span><span class="str">"one item: {single}"</span>\n        <span class="kw">case</span> [first, second]:\n            <span class="kw">return</span> <span class="fn">f</span><span class="str">"pair: {first}, {second}"</span>\n        <span class="kw">case</span> [head, *tail]:\n            <span class="kw">return</span> <span class="fn">f</span><span class="str">"head={head}, rest of length {len(tail)}"</span>\n\n<span class="fn">print</span>(<span class="fn">describe</span>([]))              <span class="cm"># empty</span>\n<span class="fn">print</span>(<span class="fn">describe</span>([<span class="num">7</span>]))             <span class="cm"># one item: 7</span>\n<span class="fn">print</span>(<span class="fn">describe</span>([<span class="num">1</span>, <span class="num">2</span>]))          <span class="cm"># pair: 1, 2</span>\n<span class="fn">print</span>(<span class="fn">describe</span>([<span class="num">1</span>, <span class="num">2</span>, <span class="num">3</span>, <span class="num">4</span>]))    <span class="cm"># head=1, rest of length 3</span></code></pre></div>'

+ '<p class="l-text"><strong>Mapping and class patterns</strong> — Match against dict shape, or against a class with attribute extraction. This is the killer feature when parsing JSON-shaped data or dispatching on AST/event objects:</p>'

+ '<div class="code-wrap"><div class="code-label"><span>PYTHON</span><button class="copy-btn" onclick="navigator.clipboard.writeText(this.closest(\'.code-wrap\').querySelector(\'code\').textContent)">COPY</button></div><pre class="code-block"><code><span class="kw">from</span> dataclasses <span class="kw">import</span> dataclass\n\n<span class="kw">@dataclass</span>\n<span class="kw">class</span> <span class="fn">Click</span>:\n    x: <span class="ty">int</span>\n    y: <span class="ty">int</span>\n\n<span class="kw">@dataclass</span>\n<span class="kw">class</span> <span class="fn">KeyPress</span>:\n    key: <span class="ty">str</span>\n    modifier: <span class="ty">str</span> = <span class="str">""</span>\n\n<span class="kw">def</span> <span class="fn">handle</span>(event):\n    <span class="kw">match</span> event:\n        <span class="cm"># Mapping pattern — destructure a dict by keys</span>\n        <span class="kw">case</span> {<span class="str">"type"</span>: <span class="str">"login"</span>, <span class="str">"user"</span>: user}:\n            <span class="kw">return</span> <span class="fn">f</span><span class="str">"{user} signed in"</span>\n        <span class="kw">case</span> {<span class="str">"type"</span>: <span class="str">"error"</span>, <span class="str">"code"</span>: code, **rest}:\n            <span class="kw">return</span> <span class="fn">f</span><span class="str">"err {code}, extra={rest}"</span>\n\n        <span class="cm"># Class pattern — match type AND extract attributes</span>\n        <span class="kw">case</span> <span class="fn">Click</span>(x=<span class="num">0</span>, y=<span class="num">0</span>):\n            <span class="kw">return</span> <span class="str">"clicked the origin"</span>\n        <span class="kw">case</span> <span class="fn">Click</span>(x=x, y=y) <span class="kw">if</span> x == y:\n            <span class="kw">return</span> <span class="fn">f</span><span class="str">"diagonal click at {x}"</span>\n        <span class="kw">case</span> <span class="fn">Click</span>(x=x, y=y):\n            <span class="kw">return</span> <span class="fn">f</span><span class="str">"click at ({x},{y})"</span>\n        <span class="kw">case</span> <span class="fn">KeyPress</span>(key=<span class="str">"Escape"</span>):\n            <span class="kw">return</span> <span class="str">"closing"</span>\n        <span class="kw">case</span> <span class="fn">KeyPress</span>(key=k, modifier=<span class="str">"ctrl"</span>):\n            <span class="kw">return</span> <span class="fn">f</span><span class="str">"shortcut: Ctrl+{k}"</span>\n        <span class="kw">case</span> _:\n            <span class="kw">return</span> <span class="str">"ignored"</span>\n\n<span class="fn">print</span>(<span class="fn">handle</span>({<span class="str">"type"</span>: <span class="str">"login"</span>, <span class="str">"user"</span>: <span class="str">"ada"</span>}))     <span class="cm"># ada signed in</span>\n<span class="fn">print</span>(<span class="fn">handle</span>(<span class="fn">Click</span>(<span class="num">10</span>, <span class="num">10</span>)))                       <span class="cm"># diagonal click at 10</span>\n<span class="fn">print</span>(<span class="fn">handle</span>(<span class="fn">KeyPress</span>(<span class="str">"s"</span>, <span class="str">"ctrl"</span>)))                    <span class="cm"># shortcut: Ctrl+s</span></code></pre></div>'

+ '<div class="think-box"><strong>When to use it:</strong> The moment you find yourself writing <code>isinstance(x, T) and x.field == ...</code> twice in a row, you are simulating <code>match</code> by hand. Refactor. The compiled bytecode for <code>match</code> is also faster than equivalent <code>isinstance</code> ladders.</div>'


/* ============================================================
   SECTION 3: TYPE HINTS
   ============================================================ */
+ '<h2 class="l-title">3. Type Hints — Modern Syntax</h2>'

+ '<div class="calc-highlight">Type hints are not enforced at runtime — they are <em>documentation that tools can check</em>. With <code>mypy</code> or <code>pyright</code>, a typed codebase catches bugs that would otherwise blow up in production. Modern syntax (3.10+) drops the awkward <code>typing.List</code> / <code>typing.Optional</code> imports in favor of built-in generics and the <code>|</code> operator.</div>'

+ '<p class="l-text"><strong>The essentials</strong> — built-in generics, union with <code>|</code>, optional, callable, type aliases:</p>'

+ '<div class="code-wrap"><div class="code-label"><span>PYTHON</span><button class="copy-btn" onclick="navigator.clipboard.writeText(this.closest(\'.code-wrap\').querySelector(\'code\').textContent)">COPY</button></div><pre class="code-block"><code><span class="kw">from</span> collections.abc <span class="kw">import</span> Callable, Iterable\n\n<span class="cm"># 3.9+ : built-in generics, no `typing.List` needed</span>\n<span class="kw">def</span> <span class="fn">word_counts</span>(words: <span class="ty">list</span>[<span class="ty">str</span>]) -> <span class="ty">dict</span>[<span class="ty">str</span>, <span class="ty">int</span>]:\n    counts: <span class="ty">dict</span>[<span class="ty">str</span>, <span class="ty">int</span>] = {}\n    <span class="kw">for</span> w <span class="kw">in</span> words:\n        counts[w] = counts.get(w, <span class="num">0</span>) + <span class="num">1</span>\n    <span class="kw">return</span> counts\n\n<span class="cm"># 3.10+ : `X | Y` replaces `Union[X, Y]`,  `X | None` replaces `Optional[X]`</span>\n<span class="kw">def</span> <span class="fn">parse_int</span>(s: <span class="ty">str</span>) -> <span class="ty">int</span> | <span class="kw">None</span>:\n    <span class="kw">try</span>:\n        <span class="kw">return</span> <span class="fn">int</span>(s)\n    <span class="kw">except</span> <span class="fn">ValueError</span>:\n        <span class="kw">return</span> <span class="kw">None</span>\n\n<span class="cm"># Callable[[arg_types], return_type]</span>\n<span class="kw">def</span> <span class="fn">apply_twice</span>(f: Callable[[<span class="ty">int</span>], <span class="ty">int</span>], x: <span class="ty">int</span>) -> <span class="ty">int</span>:\n    <span class="kw">return</span> f(f(x))\n\n<span class="cm"># Iterable is more flexible than list — accepts generators, sets, tuples</span>\n<span class="kw">def</span> <span class="fn">average</span>(nums: Iterable[<span class="ty">float</span>]) -> <span class="ty">float</span>:\n    total, count = <span class="num">0.0</span>, <span class="num">0</span>\n    <span class="kw">for</span> n <span class="kw">in</span> nums:\n        total += n\n        count += <span class="num">1</span>\n    <span class="kw">return</span> total / count <span class="kw">if</span> count <span class="kw">else</span> <span class="num">0.0</span>\n\n<span class="cm"># Type alias (3.12+ uses `type` statement)</span>\n<span class="kw">type</span> Vector = <span class="ty">list</span>[<span class="ty">float</span>]\n<span class="kw">type</span> Matrix = <span class="ty">list</span>[Vector]\n\n<span class="kw">def</span> <span class="fn">dot</span>(a: Vector, b: Vector) -> <span class="ty">float</span>:\n    <span class="kw">return</span> <span class="fn">sum</span>(x*y <span class="kw">for</span> x, y <span class="kw">in</span> <span class="fn">zip</span>(a, b))</code></pre></div>'

+ '<p class="l-text"><strong>Generics with <code>TypeVar</code></strong> — When a function is generic over a type, declare a <code>TypeVar</code> and use it. Python 3.12 simplifies the syntax further with the new <code>def f[T](...)</code> form:</p>'

+ '<div class="code-wrap"><div class="code-label"><span>PYTHON</span><button class="copy-btn" onclick="navigator.clipboard.writeText(this.closest(\'.code-wrap\').querySelector(\'code\').textContent)">COPY</button></div><pre class="code-block"><code><span class="kw">from</span> typing <span class="kw">import</span> TypeVar\n\nT = <span class="fn">TypeVar</span>(<span class="str">"T"</span>)\n\n<span class="kw">def</span> <span class="fn">first</span>(items: <span class="ty">list</span>[T]) -> T:\n    <span class="kw">return</span> items[<span class="num">0</span>]\n\n<span class="cm"># mypy infers the right return type for each call</span>\nx: <span class="ty">int</span> = <span class="fn">first</span>([<span class="num">1</span>, <span class="num">2</span>, <span class="num">3</span>])      <span class="cm"># T = int</span>\ns: <span class="ty">str</span> = <span class="fn">first</span>([<span class="str">"a"</span>, <span class="str">"b"</span>])         <span class="cm"># T = str</span>\n\n<span class="cm"># Python 3.12+ shorthand — no TypeVar import</span>\n<span class="kw">def</span> <span class="fn">last</span>[U](items: <span class="ty">list</span>[U]) -> U:\n    <span class="kw">return</span> items[-<span class="num">1</span>]</code></pre></div>'

+ '<div class="l-note"><strong>Tooling:</strong> Annotate first, check second. Run <code>mypy src/</code> or <code>pyright</code> in CI. Both will flag <code>parse_int("x") + 1</code> as an error because the result could be <code>None</code>. Catching this statically beats a 3 AM <code>TypeError</code> in production.</div>'


/* ============================================================
   SECTION 4: PATHLIB
   ============================================================ */
+ '<h2 class="l-title">4. pathlib — Retire os.path Forever</h2>'

+ '<div class="calc-highlight"><code>pathlib.Path</code> treats filesystem paths as first-class objects with methods. No more string concatenation, no more <code>os.path.join(os.path.dirname(...), ...)</code> spaghetti. It works on Windows and POSIX with the same code.</div>'

+ '<p class="l-text"><strong>The core moves</strong> — construct, navigate, query:</p>'

+ '<div class="code-wrap"><div class="code-label"><span>PYTHON</span><button class="copy-btn" onclick="navigator.clipboard.writeText(this.closest(\'.code-wrap\').querySelector(\'code\').textContent)">COPY</button></div><pre class="code-block"><code><span class="kw">from</span> pathlib <span class="kw">import</span> Path\n\n<span class="cm"># Construction — slashes, even on Windows</span>\np = <span class="fn">Path</span>(<span class="str">"data"</span>) / <span class="str">"raw"</span> / <span class="str">"corpus.txt"</span>\n<span class="fn">print</span>(p)         <span class="cm"># data/raw/corpus.txt  (or data\\raw\\corpus.txt on Windows)</span>\n\n<span class="cm"># Path components</span>\n<span class="fn">print</span>(p.parent)     <span class="cm"># Path("data/raw")</span>\n<span class="fn">print</span>(p.name)       <span class="cm"># "corpus.txt"</span>\n<span class="fn">print</span>(p.stem)       <span class="cm"># "corpus"  (name without suffix)</span>\n<span class="fn">print</span>(p.suffix)     <span class="cm"># ".txt"</span>\n<span class="fn">print</span>(p.parts)      <span class="cm"># ("data", "raw", "corpus.txt")</span>\n\n<span class="cm"># Predicates</span>\np.exists()            <span class="cm"># bool</span>\np.is_file()           <span class="cm"># bool</span>\np.is_dir()            <span class="cm"># bool</span>\n\n<span class="cm"># Create / remove</span>\n<span class="fn">Path</span>(<span class="str">"output/logs"</span>).mkdir(parents=<span class="kw">True</span>, exist_ok=<span class="kw">True</span>)\np.unlink(missing_ok=<span class="kw">True</span>)   <span class="cm"># delete file, no error if missing</span></code></pre></div>'

+ '<p class="l-text"><strong>Reading and writing</strong> — One-liners replace the open/read/close dance for small files:</p>'

+ '<div class="code-wrap"><div class="code-label"><span>PYTHON</span><button class="copy-btn" onclick="navigator.clipboard.writeText(this.closest(\'.code-wrap\').querySelector(\'code\').textContent)">COPY</button></div><pre class="code-block"><code><span class="kw">from</span> pathlib <span class="kw">import</span> Path\n\nconfig = <span class="fn">Path</span>(<span class="str">"config.json"</span>)\n\n<span class="cm"># Read entire file as text or bytes</span>\ntext = config.read_text(encoding=<span class="str">"utf-8"</span>)\nraw  = config.read_bytes()\n\n<span class="cm"># Write — atomic-ish, opens/closes for you</span>\n<span class="fn">Path</span>(<span class="str">"output/result.txt"</span>).write_text(<span class="str">"done\\n"</span>, encoding=<span class="str">"utf-8"</span>)\n\n<span class="cm"># Glob — find files by pattern, recursively with **</span>\nroot = <span class="fn">Path</span>(<span class="str">"data"</span>)\n<span class="kw">for</span> csv <span class="kw">in</span> root.glob(<span class="str">"**/*.csv"</span>):\n    <span class="fn">print</span>(csv, csv.stat().st_size)\n\n<span class="cm"># Replace suffix without string surgery</span>\nsrc = <span class="fn">Path</span>(<span class="str">"model.ckpt"</span>)\ndst = src.with_suffix(<span class="str">".safetensors"</span>)   <span class="cm"># Path("model.safetensors")</span>\n\n<span class="cm"># Resolve a path absolutely</span>\n<span class="fn">print</span>(<span class="fn">Path</span>(<span class="str">"."</span>).resolve())</code></pre></div>'

+ '<div class="l-note"><strong>Migration rule:</strong> If your fingers type <code>os.path.</code>, stop. Whatever you wanted is on a <code>Path</code> object, usually with a shorter name. The one place <code>os</code> still wins: <code>os.environ</code> for env vars.</div>'


/* ============================================================
   SECTION 5: WALRUS OPERATOR
   ============================================================ */
+ '<h2 class="l-title">5. Walrus Operator — :=</h2>'

+ '<div class="calc-highlight">The walrus operator (<code>:=</code>, official name "assignment expression," 3.8+) lets you assign and use a value in the same expression. It is small but cuts down on the most annoying boilerplate pattern: call something, store it, then immediately check it.</div>'

+ '<p class="l-text"><strong>Reading until EOF</strong> — The classic use case:</p>'

+ '<div class="code-wrap"><div class="code-label"><span>PYTHON</span><button class="copy-btn" onclick="navigator.clipboard.writeText(this.closest(\'.code-wrap\').querySelector(\'code\').textContent)">COPY</button></div><pre class="code-block"><code><span class="cm"># Before — read called twice, easy to forget the second one</span>\nchunk = f.read(<span class="num">4096</span>)\n<span class="kw">while</span> chunk:\n    process(chunk)\n    chunk = f.read(<span class="num">4096</span>)\n\n<span class="cm"># After — single call, single source of truth</span>\n<span class="kw">while</span> chunk := f.read(<span class="num">4096</span>):\n    process(chunk)</code></pre></div>'

+ '<p class="l-text"><strong>Comprehension filters</strong> — When you need to filter <em>and</em> use the computed value, the walrus saves a recomputation:</p>'

+ '<div class="code-wrap"><div class="code-label"><span>PYTHON</span><button class="copy-btn" onclick="navigator.clipboard.writeText(this.closest(\'.code-wrap\').querySelector(\'code\').textContent)">COPY</button></div><pre class="code-block"><code><span class="cm"># BAD — expensive_score() called twice per item</span>\nscored = [(x, expensive_score(x)) <span class="kw">for</span> x <span class="kw">in</span> items <span class="kw">if</span> expensive_score(x) > <span class="num">0.5</span>]\n\n<span class="cm"># GOOD — compute once, reuse in both filter and result</span>\nscored = [(x, s) <span class="kw">for</span> x <span class="kw">in</span> items <span class="kw">if</span> (s := expensive_score(x)) > <span class="num">0.5</span>]\n\n<span class="cm"># Regex match + use, no nesting</span>\n<span class="kw">import</span> re\n<span class="kw">if</span> m := re.search(<span class="str">r"id=(\\d+)"</span>, url):\n    user_id = m.group(<span class="num">1</span>)</code></pre></div>'

+ '<div class="l-note"><strong>Don\'t overuse it:</strong> If a walrus saves one line but adds a second name to track, it is worth it. If it crams three operations into a line that needs a comment to read, write the boring version. Clarity outranks cleverness.</div>'


/* ============================================================
   SECTION 6: ADVANCED F-STRINGS
   ============================================================ */
+ '<h2 class="l-title">6. f-strings — The Power Features</h2>'

+ '<div class="calc-highlight">f-strings replaced <code>.format()</code> and <code>%</code>-formatting years ago, but most code uses 10% of their capability. The format spec mini-language handles numbers, alignment, debugging, and repr — usually faster than the alternatives.</div>'

+ '<div class="code-wrap"><div class="code-label"><span>PYTHON</span><button class="copy-btn" onclick="navigator.clipboard.writeText(this.closest(\'.code-wrap\').querySelector(\'code\').textContent)">COPY</button></div><pre class="code-block"><code>pi = <span class="num">3.14159265</span>\nname = <span class="str">"ada"</span>\n\n<span class="cm"># Numeric precision and width</span>\n<span class="fn">print</span>(<span class="fn">f</span><span class="str">"{pi:.2f}"</span>)        <span class="cm"># 3.14         — fixed-point, 2 decimals</span>\n<span class="fn">print</span>(<span class="fn">f</span><span class="str">"{pi:.4e}"</span>)        <span class="cm"># 3.1416e+00   — scientific</span>\n<span class="fn">print</span>(<span class="fn">f</span><span class="str">"{pi:10.2f}"</span>)      <span class="cm"># "      3.14" — width 10, right-aligned</span>\n<span class="fn">print</span>(<span class="fn">f</span><span class="str">"{<span class="num">1234567</span>:,}"</span>)    <span class="cm"># 1,234,567    — thousands separator</span>\n<span class="fn">print</span>(<span class="fn">f</span><span class="str">"{<span class="num">0.875</span>:.1%}"</span>)     <span class="cm"># 87.5%        — percent format</span>\n\n<span class="cm"># Alignment: < left, > right, ^ center, optional fill char</span>\n<span class="fn">print</span>(<span class="fn">f</span><span class="str">"|{name:&lt;10}|"</span>)     <span class="cm"># |ada       |</span>\n<span class="fn">print</span>(<span class="fn">f</span><span class="str">"|{name:&gt;10}|"</span>)     <span class="cm"># |       ada|</span>\n<span class="fn">print</span>(<span class="fn">f</span><span class="str">"|{name:^10}|"</span>)     <span class="cm"># |   ada    |</span>\n<span class="fn">print</span>(<span class="fn">f</span><span class="str">"|{name:*^10}|"</span>)    <span class="cm"># |***ada****|</span>\n\n<span class="cm"># !r → repr(), !s → str() — useful for logging</span>\nmsg = <span class="str">"hi\\tthere"</span>\n<span class="fn">print</span>(<span class="fn">f</span><span class="str">"got: {msg!r}"</span>)      <span class="cm"># got: \'hi\\tthere\'   — escapes visible</span>\n\n<span class="cm"># 3.8+ self-documenting `=` — life-changing for debug prints</span>\nx, y = <span class="num">7</span>, <span class="num">42</span>\n<span class="fn">print</span>(<span class="fn">f</span><span class="str">"{x=}, {y=}, {x+y=}"</span>)  <span class="cm"># x=7, y=42, x+y=49</span>\n\n<span class="cm"># Date formatting via __format__</span>\n<span class="kw">from</span> datetime <span class="kw">import</span> datetime\nnow = datetime.now()\n<span class="fn">print</span>(<span class="fn">f</span><span class="str">"{now:%Y-%m-%d %H:%M}"</span>)  <span class="cm"># 2026-05-12 14:30</span></code></pre></div>'

+ '<div class="l-note"><strong>Debug f-strings</strong> (<code>f"{var=}"</code>) are the fastest way to instrument a function. Drop a few in, read the output, delete them. Beats <code>print("var =", var)</code> by a mile and survives refactors when you rename <code>var</code>.</div>'


/* ============================================================
   SECTION 7: __slots__
   ============================================================ */
+ '<h2 class="l-title">7. __slots__ — Memory Savings</h2>'

+ '<div class="calc-highlight">By default, every Python instance carries a <code>__dict__</code> for attribute storage. That dictionary is flexible — you can add fields on the fly — but it costs roughly 100 bytes per instance. For classes you create in the millions (tokens, edges, particles), <code>__slots__</code> drops the dict and stores attributes in a fixed array, cutting per-instance memory by 40–50%.</div>'

+ '<div class="code-wrap"><div class="code-label"><span>PYTHON</span><button class="copy-btn" onclick="navigator.clipboard.writeText(this.closest(\'.code-wrap\').querySelector(\'code\').textContent)">COPY</button></div><pre class="code-block"><code><span class="kw">import</span> sys\n\n<span class="kw">class</span> <span class="fn">PointA</span>:                <span class="cm"># default — has __dict__</span>\n    <span class="kw">def</span> <span class="fn">__init__</span>(<span class="kw">self</span>, x, y):\n        <span class="kw">self</span>.x = x\n        <span class="kw">self</span>.y = y\n\n<span class="kw">class</span> <span class="fn">PointB</span>:                <span class="cm"># slotted — fixed attributes only</span>\n    __slots__ = (<span class="str">"x"</span>, <span class="str">"y"</span>)\n    <span class="kw">def</span> <span class="fn">__init__</span>(<span class="kw">self</span>, x, y):\n        <span class="kw">self</span>.x = x\n        <span class="kw">self</span>.y = y\n\na = <span class="fn">PointA</span>(<span class="num">1</span>, <span class="num">2</span>)\nb = <span class="fn">PointB</span>(<span class="num">1</span>, <span class="num">2</span>)\n\n<span class="fn">print</span>(sys.getsizeof(a) + sys.getsizeof(a.__dict__))  <span class="cm"># ~152 bytes</span>\n<span class="fn">print</span>(sys.getsizeof(b))                              <span class="cm"># ~48 bytes</span>\n\n<span class="cm"># Trade-off: cannot add new attributes</span>\n<span class="cm"># b.z = 3  # AttributeError — \'z\' not in __slots__</span>\n\n<span class="cm"># Dataclass shortcut — generates __slots__ for you (3.10+)</span>\n<span class="kw">from</span> dataclasses <span class="kw">import</span> dataclass\n<span class="kw">@dataclass</span>(slots=<span class="kw">True</span>)\n<span class="kw">class</span> <span class="fn">PointC</span>:\n    x: <span class="ty">float</span>\n    y: <span class="ty">float</span></code></pre></div>'

/* --- Plotly: Memory comparison --- */
+ '<div class="calc-graph"><div class="cg-title">Per-instance Memory: Plain Class vs __slots__</div></div>'
+ '<div id="plot-python7-slots-en" class="plotly-graph"></div>'
+ '<script>setTimeout(function(){'
+ '(function(){'
+ 'var labels = ["Plain class (+ __dict__)", "__slots__ class", "@dataclass(slots=True)"];'
+ 'var bytes = [152, 48, 48];'
+ 'var colors = ["#ff6b6b", "#4ecdc4", "#c8a96e"];'
+ 'var trace = {x:labels, y:bytes, type:"bar", marker:{color:colors}, text:bytes.map(function(b){return b+" B";}), textposition:"auto"};'
+ 'var layout = {paper_bgcolor:"transparent", plot_bgcolor:"transparent",'
+ 'font:{color:"#ccc", size:14}, yaxis:{title:"Bytes per instance", gridcolor:"rgba(255,255,255,0.07)"},'
+ 'xaxis:{gridcolor:"rgba(255,255,255,0.07)"},'
+ 'margin:{l:60,r:20,t:10,b:60}};'
+ 'Plotly.newPlot("plot-python7-slots-en",[trace],layout,{responsive:true});'
+ '})();'
+ '},250)</script>'

+ '<div class="l-note"><strong>When it matters:</strong> A million-token tokenizer. A graph with millions of nodes. A simulation with millions of particles. For ten objects, <code>__slots__</code> is noise. For ten million, it is the difference between fitting in RAM and swapping to disk.</div>'


/* ============================================================
   SECTION 8: PROTOCOL — STRUCTURAL TYPING
   ============================================================ */
+ '<h2 class="l-title">8. typing.Protocol — Duck Typing With Checks</h2>'

+ '<div class="calc-highlight">Python has always done duck typing: "if it quacks, it is a duck." The downside was that type checkers had no way to express "anything with a <code>.read()</code> method." <code>Protocol</code> (PEP 544, 3.8+) fixes this — declare the shape, and any class with matching methods conforms <em>without inheriting</em>.</div>'

+ '<div class="code-wrap"><div class="code-label"><span>PYTHON</span><button class="copy-btn" onclick="navigator.clipboard.writeText(this.closest(\'.code-wrap\').querySelector(\'code\').textContent)">COPY</button></div><pre class="code-block"><code><span class="kw">from</span> typing <span class="kw">import</span> Protocol\n\n<span class="kw">class</span> <span class="fn">SupportsRead</span>(Protocol):\n    <span class="kw">def</span> <span class="fn">read</span>(<span class="kw">self</span>, n: <span class="ty">int</span> = -<span class="num">1</span>) -> <span class="ty">str</span>: ...\n\n<span class="kw">def</span> <span class="fn">load_first_kb</span>(source: SupportsRead) -> <span class="ty">str</span>:\n    <span class="kw">return</span> source.read(<span class="num">1024</span>)\n\n<span class="cm"># Any object with a `read(n)` method works — no inheritance required</span>\n<span class="kw">from</span> io <span class="kw">import</span> StringIO\nbuf = <span class="fn">StringIO</span>(<span class="str">"hello world"</span>)\n<span class="fn">print</span>(<span class="fn">load_first_kb</span>(buf))           <span class="cm"># "hello world"</span>\n\n<span class="kw">with</span> <span class="fn">open</span>(<span class="str">"data.txt"</span>) <span class="kw">as</span> f:\n    <span class="fn">load_first_kb</span>(f)                  <span class="cm"># also fits the protocol</span>\n\n<span class="cm"># Runtime-checkable variant</span>\n<span class="kw">from</span> typing <span class="kw">import</span> runtime_checkable\n\n<span class="kw">@runtime_checkable</span>\n<span class="kw">class</span> <span class="fn">Sized</span>(Protocol):\n    <span class="kw">def</span> <span class="fn">__len__</span>(<span class="kw">self</span>) -> <span class="ty">int</span>: ...\n\n<span class="fn">print</span>(<span class="fn">isinstance</span>([<span class="num">1</span>, <span class="num">2</span>, <span class="num">3</span>], <span class="fn">Sized</span>))   <span class="cm"># True — list has __len__</span>\n<span class="fn">print</span>(<span class="fn">isinstance</span>(<span class="num">42</span>, <span class="fn">Sized</span>))           <span class="cm"># False — int has no __len__</span></code></pre></div>'

+ '<p class="l-text"><code>collections.abc</code> ships pre-built protocols: <code>Iterable</code>, <code>Iterator</code>, <code>Sized</code>, <code>Container</code>, <code>Hashable</code>, <code>Mapping</code>, <code>Sequence</code>. Reach for those first. Write your own <code>Protocol</code> when you need a domain-specific shape — for example, "anything with a <code>.predict(X)</code> method" for a generic model evaluator.</p>'

+ '<div class="l-note"><strong>The takeaway:</strong> Protocols are how grown-up Python codebases get the flexibility of duck typing <em>and</em> the safety of static checks. Concrete inheritance (<code>class Cat(Animal)</code>) for "is a"; protocols for "looks like."</div>'


/* ============================================================
   SECTION 9: MODERN PACKAGING
   ============================================================ */
+ '<h2 class="l-title">9. Modern Packaging — pyproject.toml + uv</h2>'

+ '<div class="calc-highlight">For a decade the answer to "how do I package this?" involved <code>setup.py</code>, <code>requirements.txt</code>, <code>setup.cfg</code>, <code>MANIFEST.in</code>, and a virtual environment you forgot to activate. The modern answer is one file (<code>pyproject.toml</code>) and one tool (<code>uv</code>).</div>'

+ '<p class="l-text"><strong>pyproject.toml</strong> — the single source of truth for project metadata, dependencies, and tool config:</p>'

+ '<div class="code-wrap"><div class="code-label"><span>TOML</span><button class="copy-btn" onclick="navigator.clipboard.writeText(this.closest(\'.code-wrap\').querySelector(\'code\').textContent)">COPY</button></div><pre class="code-block"><code><span class="cm">[project]</span>\nname = <span class="str">"nlp-pipeline"</span>\nversion = <span class="str">"0.1.0"</span>\ndescription = <span class="str">"Cleans and tokenizes corpora."</span>\nreadme = <span class="str">"README.md"</span>\nrequires-python = <span class="str">"&gt;=3.11"</span>\ndependencies = [\n    <span class="str">"numpy&gt;=2.0"</span>,\n    <span class="str">"pandas&gt;=2.2"</span>,\n    <span class="str">"transformers&gt;=4.40"</span>,\n]\n\n[project.optional-dependencies]\ndev = [<span class="str">"pytest"</span>, <span class="str">"ruff"</span>, <span class="str">"mypy"</span>]\n\n[project.scripts]\nrun-pipeline = <span class="str">"nlp_pipeline.cli:main"</span>\n\n[build-system]\nrequires = [<span class="str">"hatchling"</span>]\nbuild-backend = <span class="str">"hatchling.build"</span>\n\n[tool.ruff]\nline-length = 100\ntarget-version = <span class="str">"py311"</span>\n\n[tool.mypy]\nstrict = <span class="kw">true</span></code></pre></div>'

+ '<p class="l-text"><strong>uv — the new package manager</strong>. Rust-based, 10–100x faster than pip, manages Python versions and virtual environments in one tool:</p>'

+ '<div class="code-wrap"><div class="code-label"><span>SHELL</span><button class="copy-btn" onclick="navigator.clipboard.writeText(this.closest(\'.code-wrap\').querySelector(\'code\').textContent)">COPY</button></div><pre class="code-block"><code><span class="cm"># Bootstrap a new project — creates pyproject.toml + .venv</span>\nuv init nlp-pipeline\n<span class="kw">cd</span> nlp-pipeline\n\n<span class="cm"># Add a dependency — updates pyproject.toml AND installs</span>\nuv add numpy pandas transformers\nuv add --dev pytest ruff mypy\n\n<span class="cm"># Run a command inside the project venv (no manual activation)</span>\nuv run python train.py\nuv run pytest\n\n<span class="cm"># Lock dependencies for reproducible installs</span>\nuv lock                <span class="cm"># writes uv.lock</span>\nuv sync                <span class="cm"># installs exactly what is in uv.lock</span>\n\n<span class="cm"># Install a different Python version</span>\nuv python install 3.12\nuv venv --python 3.12\n\n<span class="cm"># Run a one-off script with isolated deps (no project needed)</span>\nuv run --with rich --with httpx scratch.py</code></pre></div>'

+ '<div class="l-note"><strong>Migration cheat sheet:</strong> <code>pip install X</code> &rarr; <code>uv add X</code>. <code>python -m venv .venv</code> &rarr; <code>uv venv</code>. <code>pip freeze &gt; requirements.txt</code> &rarr; <code>uv lock</code>. <code>python script.py</code> &rarr; <code>uv run script.py</code>. The mental model is: <em>uv owns the environment, you just declare what you want</em>.</div>'


/* ============================================================
   SECTION 10: 2026 BEST PRACTICES
   ============================================================ */
+ '<h2 class="l-title">10. The 2026 Python Toolchain</h2>'

+ '<div class="calc-highlight">A modern Python project is not just code — it is code plus a small army of tools that catch problems before humans do. The list below is the de facto standard. Configure them once, commit the configs, never think about them again until they save you from a bug.</div>'

+ '<div class="calc-cards">'
+ '<div class="calc-card"><div class="cc-icon">uv</div><div class="cc-title">Environments &amp; deps</div><div class="cc-body"><code>uv</code> replaces pip, pip-tools, venv, pyenv, and pipx. Single tool, one config file, blistering speed.</div></div>'
+ '<div class="calc-card"><div class="cc-icon">ruff</div><div class="cc-title">Lint + Format</div><div class="cc-body"><code>ruff check</code> replaces flake8/isort/pylint. <code>ruff format</code> replaces black. Rust-fast, one config in <code>pyproject.toml</code>.</div></div>'
+ '<div class="calc-card"><div class="cc-icon">mypy</div><div class="cc-title">Static type check</div><div class="cc-body"><code>mypy --strict</code> or <code>pyright</code>. Run in CI. Catches the bugs no human reviewer reliably catches.</div></div>'
+ '<div class="calc-card"><div class="cc-icon">pytest</div><div class="cc-title">Testing</div><div class="cc-body"><code>pytest</code> + fixtures + parametrize. <code>pytest-cov</code> for coverage. <code>hypothesis</code> for property-based tests.</div></div>'
+ '<div class="calc-card"><div class="cc-icon">pre</div><div class="cc-title">pre-commit</div><div class="cc-body">Runs <code>ruff</code>, <code>mypy</code>, secrets scanner before every commit. Stops bad code at the door.</div></div>'
+ '<div class="calc-card"><div class="cc-icon">CI</div><div class="cc-title">GitHub Actions</div><div class="cc-body">One workflow: install <code>uv</code>, <code>uv sync</code>, <code>uv run ruff check</code>, <code>uv run mypy</code>, <code>uv run pytest</code>. Five minutes from clone to verdict.</div></div>'
+ '</div>'

+ '<p class="l-text"><strong>Minimal <code>ruff</code> + <code>mypy</code> config (drop into <code>pyproject.toml</code>):</strong></p>'

+ '<div class="code-wrap"><div class="code-label"><span>TOML</span><button class="copy-btn" onclick="navigator.clipboard.writeText(this.closest(\'.code-wrap\').querySelector(\'code\').textContent)">COPY</button></div><pre class="code-block"><code>[tool.ruff]\nline-length = 100\ntarget-version = <span class="str">"py311"</span>\n\n[tool.ruff.lint]\nselect = [<span class="str">"E"</span>, <span class="str">"F"</span>, <span class="str">"W"</span>, <span class="str">"I"</span>, <span class="str">"N"</span>, <span class="str">"UP"</span>, <span class="str">"B"</span>, <span class="str">"SIM"</span>, <span class="str">"RUF"</span>]\nignore = [<span class="str">"E501"</span>]   <span class="cm"># line length handled by formatter</span>\n\n[tool.mypy]\npython_version = <span class="str">"3.11"</span>\nstrict = <span class="kw">true</span>\nwarn_unused_ignores = <span class="kw">true</span>\nwarn_redundant_casts = <span class="kw">true</span>\n\n[tool.pytest.ini_options]\ntestpaths = [<span class="str">"tests"</span>]\naddopts = <span class="str">"-ra --strict-markers"</span></code></pre></div>'

+ '<div class="think-box"><strong>The professionalism gap:</strong> Many self-taught Python users never adopt these tools — "it works on my machine" feels like enough. The moment you join a real team or ship to real users, this toolchain stops being optional. The good news: configure it once with the snippet above, and from that day forward, the tools enforce the discipline for you.</div>'

+ '<div class="l-note"><strong>End of Python L7.</strong> You now write Python like a 2026 developer: dataclasses instead of <code>__init__</code> boilerplate, <code>match</code> instead of <code>if/elif</code> ladders, type hints with <code>|</code> and <code>list[int]</code>, <code>pathlib</code> instead of <code>os.path</code>, <code>uv</code> instead of pip, and a toolchain that catches mistakes before commits land. Carry these habits into every project — they compound.</div>'

,

/* ================================================================
   TURKISH TRANSLATION
   ================================================================ */

tr: '<p class="l-text"><strong>Modern Python, 2015\'in Python\'u değil.</strong> Dil hızla evrildi: <code>dataclasses</code> standart kalıpları öldürdü, <code>match/case</code> yapısal desen eşleştirmesini getirdi, <code>pathlib</code> <code>os.path</code>\'i emekli etti, tip ipuçları olmazsa olmaz oldu, araç ekosistemi de bir gecede <code>pip</code>+<code>venv</code>\'den <code>uv</code>\'ye geçti. Python\'u 2018 alışkanlıklarıyla yazıyorsanız, daha ilk gün eski kod yazıyorsunuz.</p>'

+ '<p class="l-text">Bu ders, <em>2026 deyimsel Python\'unu</em> tanımlayan özelliklerin ve araçların turu. Her bölüm ara sıra değil, günlük başvurmanız gereken bir araç. Bunları atlarsanız kodunuz olduğundan on yaş daha eski görünür: uzun, kırılgan, refaktör etmesi zor.</p>'

+ '<div class="lesson-outcomes" style="background:rgba(200,169,110,0.06);border-left:3px solid #c8a96e;padding:1rem 1.2rem;margin:1.5rem 0;border-radius:0 8px 8px 0"><div style="font-size:0.78rem;font-weight:700;letter-spacing:0.08em;color:#c8a96e;margin-bottom:0.6rem">📍 BU DERSTE ÖĞRENECEKSİN</div><ul style="margin:0;padding-left:1.3rem;line-height:1.65;font-size:0.93rem;color:rgba(235,230,220,0.92)"><li>Standart <code>__init__</code> kalıbı yerine tek satırda <code>@dataclass</code> kullan</li><li>İç içe <code>if/elif</code> zincirlerini <code>match/case</code> yapısal desenleriyle yeniden yaz</li><li><code>X | Y</code>, <code>list[int]</code>, <code>Protocol</code>, <code>TypeVar</code> ile modern tip ipucu yaz</li><li><code>os.path</code>\'i bırak — dosya sistemini <code>pathlib.Path</code> ile yönet</li><li>Walrus operatörü, gelişmiş f-string ve bellek tasarrufu için <code>__slots__</code> kullan</li><li>Projeleri <code>pyproject.toml</code> ve <code>uv</code> ile yönet (yeni standart)</li></ul></div>'


/* ============================================================
   BÖLÜM 1: DATACLASSES
   ============================================================ */
+ '<h2 class="l-title">1. Dataclasses — Standart Kalıplara Veda</h2>'

+ '<div class="calc-highlight"><strong>Sorun:</strong> Çoğunlukla veri tutan bir sınıf yazmak demek aynı alanları üç kez yazmak demek — bir kez <code>__init__</code>\'te, bir kez nitelik olarak, bir kez de <code>__repr__</code>\'de. Dataclasses tek bir tanımdan bunların hepsini otomatik üretir.</div>'

+ '<p class="l-text"><strong>Önce ve sonra</strong> — On beş satırın dört satıra düşüşünü izle:</p>'

+ '<div class="code-wrap"><div class="code-label"><span>PYTHON</span><button class="copy-btn" onclick="navigator.clipboard.writeText(this.closest(\'.code-wrap\').querySelector(\'code\').textContent)">COPY</button></div><pre class="code-block"><code><span class="cm"># ÖNCE — elle yazılmış sınıf, her satır parmakla</span>\n<span class="kw">class</span> <span class="fn">Point</span>:\n    <span class="kw">def</span> <span class="fn">__init__</span>(<span class="kw">self</span>, x, y, label=<span class="str">""</span>):\n        <span class="kw">self</span>.x = x\n        <span class="kw">self</span>.y = y\n        <span class="kw">self</span>.label = label\n\n    <span class="kw">def</span> <span class="fn">__repr__</span>(<span class="kw">self</span>):\n        <span class="kw">return</span> <span class="fn">f</span><span class="str">"Point(x={self.x}, y={self.y}, label={self.label!r})"</span>\n\n    <span class="kw">def</span> <span class="fn">__eq__</span>(<span class="kw">self</span>, other):\n        <span class="kw">return</span> (<span class="kw">self</span>.x, <span class="kw">self</span>.y, <span class="kw">self</span>.label) == (other.x, other.y, other.label)\n\n<span class="cm"># SONRA — aynı davranış, tek dekoratör</span>\n<span class="kw">from</span> dataclasses <span class="kw">import</span> dataclass\n\n<span class="kw">@dataclass</span>\n<span class="kw">class</span> <span class="fn">Point</span>:\n    x: <span class="ty">float</span>\n    y: <span class="ty">float</span>\n    label: <span class="ty">str</span> = <span class="str">""</span>\n\np = <span class="fn">Point</span>(<span class="num">3</span>, <span class="num">4</span>, <span class="str">"origin"</span>)\n<span class="fn">print</span>(p)            <span class="cm"># Point(x=3, y=4, label=\'origin\')</span>\n<span class="fn">print</span>(p == <span class="fn">Point</span>(<span class="num">3</span>, <span class="num">4</span>, <span class="str">"origin"</span>))  <span class="cm"># True — otomatik __eq__</span></code></pre></div>'

+ '<p class="l-text"><code>@dataclass</code> dekoratörü sınıf annotation\'larını inceler ve <code>__init__</code>, <code>__repr__</code>, <code>__eq__</code>\'i senin için yazar. Alanlar gerçek niteliklerdir — alt sınıf yapabilir, metot ekleyebilir, istediğini ezebilirsin.</p>'

+ '<p class="l-text"><strong>Değişken varsayılan tuzağı — <code>field</code></strong> — Sakın <code>tags: list = []</code> yazma. Tüm örnekler aynı listeyi paylaşır. Yerine <code>field(default_factory=list)</code> kullan; her örnek için taze bir liste üretir:</p>'

+ '<div class="code-wrap"><div class="code-label"><span>PYTHON</span><button class="copy-btn" onclick="navigator.clipboard.writeText(this.closest(\'.code-wrap\').querySelector(\'code\').textContent)">COPY</button></div><pre class="code-block"><code><span class="kw">from</span> dataclasses <span class="kw">import</span> dataclass, field\n\n<span class="kw">@dataclass</span>\n<span class="kw">class</span> <span class="fn">Document</span>:\n    title: <span class="ty">str</span>\n    tags: <span class="ty">list</span>[<span class="ty">str</span>] = <span class="fn">field</span>(default_factory=<span class="ty">list</span>)\n    metadata: <span class="ty">dict</span>[<span class="ty">str</span>, <span class="ty">str</span>] = <span class="fn">field</span>(default_factory=<span class="ty">dict</span>)\n    word_count: <span class="ty">int</span> = <span class="num">0</span>\n\nd1 = <span class="fn">Document</span>(<span class="str">"NLP Temelleri"</span>)\nd1.tags.append(<span class="str">"giris"</span>)\n\nd2 = <span class="fn">Document</span>(<span class="str">"Transformerlar"</span>)\n<span class="fn">print</span>(d2.tags)   <span class="cm"># [] — d1 tarafından kirletilmedi, her örneğe taze liste</span></code></pre></div>'

+ '<p class="l-text"><strong>Dondurulmuş dataclasses</strong> — <code>frozen=True</code> eklediğinde örnek değişmez olur. Bu sana hediye olarak <code>__hash__</code>\'ı verir; nesne artık küme ve sözlük anahtarı olabilir — değer nesneleri, konfig anlık görüntüleri ve cache anahtarları için bire bir:</p>'

+ '<div class="code-wrap"><div class="code-label"><span>PYTHON</span><button class="copy-btn" onclick="navigator.clipboard.writeText(this.closest(\'.code-wrap\').querySelector(\'code\').textContent)">COPY</button></div><pre class="code-block"><code><span class="kw">from</span> dataclasses <span class="kw">import</span> dataclass\n\n<span class="kw">@dataclass</span>(frozen=<span class="kw">True</span>, slots=<span class="kw">True</span>)\n<span class="kw">class</span> <span class="fn">ModelConfig</span>:\n    name: <span class="ty">str</span>\n    hidden_dim: <span class="ty">int</span>\n    n_layers: <span class="ty">int</span>\n    dropout: <span class="ty">float</span> = <span class="num">0.1</span>\n\ncfg = <span class="fn">ModelConfig</span>(<span class="str">"transformer-base"</span>, <span class="num">512</span>, <span class="num">6</span>)\n<span class="cm"># cfg.dropout = 0.2  # FrozenInstanceError — mutasyon yok</span>\n\n<span class="cm"># Hashable olduğu için sözlük anahtarı olarak kullanılabilir</span>\nresults = {cfg: <span class="num">0.87</span>}\n<span class="fn">print</span>(results[<span class="fn">ModelConfig</span>(<span class="str">"transformer-base"</span>, <span class="num">512</span>, <span class="num">6</span>)])  <span class="cm"># 0.87</span></code></pre></div>'

+ '<p class="l-text"><strong><code>__post_init__</code></strong> — Otomatik üretilen <code>__init__</code> bittikten sonra bir alanı doğrulamak veya türetmek mi istiyorsun? <code>__post_init__</code> tanımla, dataclass çağırır:</p>'

+ '<div class="code-wrap"><div class="code-label"><span>PYTHON</span><button class="copy-btn" onclick="navigator.clipboard.writeText(this.closest(\'.code-wrap\').querySelector(\'code\').textContent)">COPY</button></div><pre class="code-block"><code><span class="kw">from</span> dataclasses <span class="kw">import</span> dataclass, field\n\n<span class="kw">@dataclass</span>\n<span class="kw">class</span> <span class="fn">Tensor</span>:\n    shape: <span class="ty">tuple</span>[<span class="ty">int</span>, ...]\n    dtype: <span class="ty">str</span> = <span class="str">"float32"</span>\n    n_elements: <span class="ty">int</span> = <span class="fn">field</span>(init=<span class="kw">False</span>)  <span class="cm"># türetilmiş, argüman değil</span>\n\n    <span class="kw">def</span> <span class="fn">__post_init__</span>(<span class="kw">self</span>):\n        <span class="kw">if</span> <span class="fn">any</span>(d <= <span class="num">0</span> <span class="kw">for</span> d <span class="kw">in</span> <span class="kw">self</span>.shape):\n            <span class="kw">raise</span> <span class="fn">ValueError</span>(<span class="fn">f</span><span class="str">"Gecersiz shape: {self.shape}"</span>)\n        <span class="kw">import</span> math\n        <span class="kw">self</span>.n_elements = math.prod(<span class="kw">self</span>.shape)\n\nt = <span class="fn">Tensor</span>(shape=(<span class="num">32</span>, <span class="num">128</span>, <span class="num">768</span>))\n<span class="fn">print</span>(t.n_elements)  <span class="cm"># 3145728</span></code></pre></div>'

+ '<div class="l-note"><strong>Ne zaman dataclass?</strong> Sınıf öncelikle alanlardan oluşan bir kayıt ise. Davranış baskın, alanlar tali kalıyorsa düz <code>class</code> kullan. Çalışma zamanı doğrulamalı şemalar için (API gövdesi, kullanıcı girdisi) <code>pydantic.BaseModel</code>\'a geç — dataclass fikirleri üzerine kurulur, tip dönüşümü de ekler.</div>'


/* ============================================================
   BÖLÜM 2: PATTERN MATCHING (match/case)
   ============================================================ */
+ '<h2 class="l-title">2. Pattern Matching — match/case</h2>'

+ '<div class="calc-highlight">Python 3.10 ile gelen <code>match/case</code> <em>yapısal</em> desen eşleştirmedir: <code>switch</code> gibi sadece eşitlik bakmaz — nesneleri, dizileri ve sözlükleri parçalarına ayırır, parçaları isteğe bağlı olarak isimlere bağlar. Bir kez içselleştirdiğinde iç içe <code>if/elif</code> zincirlerinin yarısı buharlaşır.</div>'

+ '<p class="l-text"><strong>Sabit ve yakalama desenleri</strong> — En basit hâli <code>switch</code> yerine geçer. Joker <code>_</code> yukarıda eşleşmeyen her şeyi yakalar:</p>'

+ '<div class="code-wrap"><div class="code-label"><span>PYTHON</span><button class="copy-btn" onclick="navigator.clipboard.writeText(this.closest(\'.code-wrap\').querySelector(\'code\').textContent)">COPY</button></div><pre class="code-block"><code><span class="kw">def</span> <span class="fn">http_status</span>(code: <span class="ty">int</span>) -> <span class="ty">str</span>:\n    <span class="kw">match</span> code:\n        <span class="kw">case</span> <span class="num">200</span> | <span class="num">201</span> | <span class="num">204</span>:\n            <span class="kw">return</span> <span class="str">"Basarili"</span>\n        <span class="kw">case</span> <span class="num">301</span> | <span class="num">302</span>:\n            <span class="kw">return</span> <span class="str">"Yonlendirme"</span>\n        <span class="kw">case</span> <span class="num">404</span>:\n            <span class="kw">return</span> <span class="str">"Bulunamadi"</span>\n        <span class="kw">case</span> n <span class="kw">if</span> <span class="num">500</span> <= n < <span class="num">600</span>:        <span class="cm"># guard (koşul) cümlesi</span>\n            <span class="kw">return</span> <span class="fn">f</span><span class="str">"Sunucu hatasi ({n})"</span>\n        <span class="kw">case</span> _:\n            <span class="kw">return</span> <span class="str">"Bilinmeyen"</span>\n\n<span class="fn">print</span>(<span class="fn">http_status</span>(<span class="num">503</span>))  <span class="cm"># "Sunucu hatasi (503)"</span></code></pre></div>'

+ '<p class="l-text"><strong>Dizi desenleri</strong> — Desenler liste ve tuple\'ları parçalayabilir, kalanı <code>*name</code> ile toplayabilir. <code>match</code>\'in <code>if</code>\'ten parladığı nokta tam burası:</p>'

+ '<div class="code-wrap"><div class="code-label"><span>PYTHON</span><button class="copy-btn" onclick="navigator.clipboard.writeText(this.closest(\'.code-wrap\').querySelector(\'code\').textContent)">COPY</button></div><pre class="code-block"><code><span class="kw">def</span> <span class="fn">describe</span>(items):\n    <span class="kw">match</span> items:\n        <span class="kw">case</span> []:\n            <span class="kw">return</span> <span class="str">"bos"</span>\n        <span class="kw">case</span> [single]:\n            <span class="kw">return</span> <span class="fn">f</span><span class="str">"tek oge: {single}"</span>\n        <span class="kw">case</span> [first, second]:\n            <span class="kw">return</span> <span class="fn">f</span><span class="str">"cift: {first}, {second}"</span>\n        <span class="kw">case</span> [head, *tail]:\n            <span class="kw">return</span> <span class="fn">f</span><span class="str">"bas={head}, kalan {len(tail)} adet"</span>\n\n<span class="fn">print</span>(<span class="fn">describe</span>([]))              <span class="cm"># bos</span>\n<span class="fn">print</span>(<span class="fn">describe</span>([<span class="num">7</span>]))             <span class="cm"># tek oge: 7</span>\n<span class="fn">print</span>(<span class="fn">describe</span>([<span class="num">1</span>, <span class="num">2</span>]))          <span class="cm"># cift: 1, 2</span>\n<span class="fn">print</span>(<span class="fn">describe</span>([<span class="num">1</span>, <span class="num">2</span>, <span class="num">3</span>, <span class="num">4</span>]))    <span class="cm"># bas=1, kalan 3 adet</span></code></pre></div>'

+ '<p class="l-text"><strong>Sözlük ve sınıf desenleri</strong> — Sözlük şekline veya bir sınıfa karşı, nitelik çıkararak eşleştir. JSON biçimli veri ayrıştırırken ya da AST/olay nesnelerine dispatch yaparken yıldız özellik:</p>'

+ '<div class="code-wrap"><div class="code-label"><span>PYTHON</span><button class="copy-btn" onclick="navigator.clipboard.writeText(this.closest(\'.code-wrap\').querySelector(\'code\').textContent)">COPY</button></div><pre class="code-block"><code><span class="kw">from</span> dataclasses <span class="kw">import</span> dataclass\n\n<span class="kw">@dataclass</span>\n<span class="kw">class</span> <span class="fn">Click</span>:\n    x: <span class="ty">int</span>\n    y: <span class="ty">int</span>\n\n<span class="kw">@dataclass</span>\n<span class="kw">class</span> <span class="fn">KeyPress</span>:\n    key: <span class="ty">str</span>\n    modifier: <span class="ty">str</span> = <span class="str">""</span>\n\n<span class="kw">def</span> <span class="fn">handle</span>(event):\n    <span class="kw">match</span> event:\n        <span class="cm"># Sözlük deseni — anahtarlara göre parçala</span>\n        <span class="kw">case</span> {<span class="str">"type"</span>: <span class="str">"login"</span>, <span class="str">"user"</span>: user}:\n            <span class="kw">return</span> <span class="fn">f</span><span class="str">"{user} giris yapti"</span>\n        <span class="kw">case</span> {<span class="str">"type"</span>: <span class="str">"error"</span>, <span class="str">"code"</span>: code, **rest}:\n            <span class="kw">return</span> <span class="fn">f</span><span class="str">"hata {code}, ek={rest}"</span>\n\n        <span class="cm"># Sınıf deseni — tipi VE niteliği aynı anda eşle</span>\n        <span class="kw">case</span> <span class="fn">Click</span>(x=<span class="num">0</span>, y=<span class="num">0</span>):\n            <span class="kw">return</span> <span class="str">"origin tiklandi"</span>\n        <span class="kw">case</span> <span class="fn">Click</span>(x=x, y=y) <span class="kw">if</span> x == y:\n            <span class="kw">return</span> <span class="fn">f</span><span class="str">"capraz tik: {x}"</span>\n        <span class="kw">case</span> <span class="fn">Click</span>(x=x, y=y):\n            <span class="kw">return</span> <span class="fn">f</span><span class="str">"tik ({x},{y})"</span>\n        <span class="kw">case</span> <span class="fn">KeyPress</span>(key=<span class="str">"Escape"</span>):\n            <span class="kw">return</span> <span class="str">"kapatiliyor"</span>\n        <span class="kw">case</span> <span class="fn">KeyPress</span>(key=k, modifier=<span class="str">"ctrl"</span>):\n            <span class="kw">return</span> <span class="fn">f</span><span class="str">"kisayol: Ctrl+{k}"</span>\n        <span class="kw">case</span> _:\n            <span class="kw">return</span> <span class="str">"yok sayildi"</span>\n\n<span class="fn">print</span>(<span class="fn">handle</span>({<span class="str">"type"</span>: <span class="str">"login"</span>, <span class="str">"user"</span>: <span class="str">"ada"</span>}))     <span class="cm"># ada giris yapti</span>\n<span class="fn">print</span>(<span class="fn">handle</span>(<span class="fn">Click</span>(<span class="num">10</span>, <span class="num">10</span>)))                       <span class="cm"># capraz tik: 10</span>\n<span class="fn">print</span>(<span class="fn">handle</span>(<span class="fn">KeyPress</span>(<span class="str">"s"</span>, <span class="str">"ctrl"</span>)))                    <span class="cm"># kisayol: Ctrl+s</span></code></pre></div>'

+ '<div class="think-box"><strong>Ne zaman?</strong> Üst üste iki kez <code>isinstance(x, T) and x.field == ...</code> yazdığını fark ettiğin an, aslında <code>match</code>\'i elle simüle ediyorsun. Refaktör et. Üstelik <code>match</code>\'in bytecode\'u eşdeğer <code>isinstance</code> merdivenlerinden daha hızlıdır.</div>'


/* ============================================================
   BÖLÜM 3: TYPE HINTS
   ============================================================ */
+ '<h2 class="l-title">3. Type Hints — Modern Sözdizimi</h2>'

+ '<div class="calc-highlight">Tip ipuçları çalışma zamanında zorlanmaz — bunlar <em>araçların kontrol edebildiği belgelerdir</em>. <code>mypy</code> veya <code>pyright</code> ile tiplenmiş bir kod tabanı, üretimde patlayacak hataları geliştirme aşamasında yakalar. Modern sözdizimi (3.10+), garip <code>typing.List</code> / <code>typing.Optional</code> import\'larını bırakıp yerleşik generic\'leri ve <code>|</code> operatörünü tercih eder.</div>'

+ '<p class="l-text"><strong>Olmazsa olmazlar</strong> — yerleşik generic\'ler, <code>|</code> ile union, optional, callable, tip takma adları:</p>'

+ '<div class="code-wrap"><div class="code-label"><span>PYTHON</span><button class="copy-btn" onclick="navigator.clipboard.writeText(this.closest(\'.code-wrap\').querySelector(\'code\').textContent)">COPY</button></div><pre class="code-block"><code><span class="kw">from</span> collections.abc <span class="kw">import</span> Callable, Iterable\n\n<span class="cm"># 3.9+ : yerleşik generic, `typing.List` import etmeye gerek yok</span>\n<span class="kw">def</span> <span class="fn">word_counts</span>(words: <span class="ty">list</span>[<span class="ty">str</span>]) -> <span class="ty">dict</span>[<span class="ty">str</span>, <span class="ty">int</span>]:\n    counts: <span class="ty">dict</span>[<span class="ty">str</span>, <span class="ty">int</span>] = {}\n    <span class="kw">for</span> w <span class="kw">in</span> words:\n        counts[w] = counts.get(w, <span class="num">0</span>) + <span class="num">1</span>\n    <span class="kw">return</span> counts\n\n<span class="cm"># 3.10+ : `X | Y` `Union[X, Y]` yerine,  `X | None` `Optional[X]` yerine</span>\n<span class="kw">def</span> <span class="fn">parse_int</span>(s: <span class="ty">str</span>) -> <span class="ty">int</span> | <span class="kw">None</span>:\n    <span class="kw">try</span>:\n        <span class="kw">return</span> <span class="fn">int</span>(s)\n    <span class="kw">except</span> <span class="fn">ValueError</span>:\n        <span class="kw">return</span> <span class="kw">None</span>\n\n<span class="cm"># Callable[[arg_tipleri], donus_tipi]</span>\n<span class="kw">def</span> <span class="fn">apply_twice</span>(f: Callable[[<span class="ty">int</span>], <span class="ty">int</span>], x: <span class="ty">int</span>) -> <span class="ty">int</span>:\n    <span class="kw">return</span> f(f(x))\n\n<span class="cm"># Iterable list\'ten daha esnek — generator, set, tuple da kabul eder</span>\n<span class="kw">def</span> <span class="fn">average</span>(nums: Iterable[<span class="ty">float</span>]) -> <span class="ty">float</span>:\n    total, count = <span class="num">0.0</span>, <span class="num">0</span>\n    <span class="kw">for</span> n <span class="kw">in</span> nums:\n        total += n\n        count += <span class="num">1</span>\n    <span class="kw">return</span> total / count <span class="kw">if</span> count <span class="kw">else</span> <span class="num">0.0</span>\n\n<span class="cm"># Tip takma adı (3.12+ `type` ifadesini kullanır)</span>\n<span class="kw">type</span> Vector = <span class="ty">list</span>[<span class="ty">float</span>]\n<span class="kw">type</span> Matrix = <span class="ty">list</span>[Vector]\n\n<span class="kw">def</span> <span class="fn">dot</span>(a: Vector, b: Vector) -> <span class="ty">float</span>:\n    <span class="kw">return</span> <span class="fn">sum</span>(x*y <span class="kw">for</span> x, y <span class="kw">in</span> <span class="fn">zip</span>(a, b))</code></pre></div>'

+ '<p class="l-text"><strong><code>TypeVar</code> ile generic fonksiyonlar</strong> — Fonksiyon bir tip üzerinde generic ise, bir <code>TypeVar</code> bildir ve kullan. Python 3.12, <code>def f[T](...)</code> yeni biçimiyle sözdizimini daha da sadeleştirir:</p>'

+ '<div class="code-wrap"><div class="code-label"><span>PYTHON</span><button class="copy-btn" onclick="navigator.clipboard.writeText(this.closest(\'.code-wrap\').querySelector(\'code\').textContent)">COPY</button></div><pre class="code-block"><code><span class="kw">from</span> typing <span class="kw">import</span> TypeVar\n\nT = <span class="fn">TypeVar</span>(<span class="str">"T"</span>)\n\n<span class="kw">def</span> <span class="fn">first</span>(items: <span class="ty">list</span>[T]) -> T:\n    <span class="kw">return</span> items[<span class="num">0</span>]\n\n<span class="cm"># mypy her çağrı için doğru dönüş tipini çıkarır</span>\nx: <span class="ty">int</span> = <span class="fn">first</span>([<span class="num">1</span>, <span class="num">2</span>, <span class="num">3</span>])      <span class="cm"># T = int</span>\ns: <span class="ty">str</span> = <span class="fn">first</span>([<span class="str">"a"</span>, <span class="str">"b"</span>])         <span class="cm"># T = str</span>\n\n<span class="cm"># Python 3.12+ kısa biçim — TypeVar import yok</span>\n<span class="kw">def</span> <span class="fn">last</span>[U](items: <span class="ty">list</span>[U]) -> U:\n    <span class="kw">return</span> items[-<span class="num">1</span>]</code></pre></div>'

+ '<div class="l-note"><strong>Araçlar:</strong> Önce annotate et, sonra kontrol et. CI\'da <code>mypy src/</code> ya da <code>pyright</code> çalıştır. İkisi de <code>parse_int("x") + 1</code>\'i hata olarak işaretler çünkü sonuç <code>None</code> olabilir. Bunu statik yakalamak, gece 3\'te üretimde <code>TypeError</code> almaktan iyidir.</div>'


/* ============================================================
   BÖLÜM 4: PATHLIB
   ============================================================ */
+ '<h2 class="l-title">4. pathlib — os.path\'i Sonsuza Dek Bırak</h2>'

+ '<div class="calc-highlight"><code>pathlib.Path</code>, dosya yollarını metotları olan birinci sınıf nesneler olarak ele alır. Ne string birleştirme ne de <code>os.path.join(os.path.dirname(...), ...)</code> spagettisi. Aynı kod hem Windows\'ta hem POSIX\'te çalışır.</div>'

+ '<p class="l-text"><strong>Temel hareketler</strong> — oluştur, gezin, sorgula:</p>'

+ '<div class="code-wrap"><div class="code-label"><span>PYTHON</span><button class="copy-btn" onclick="navigator.clipboard.writeText(this.closest(\'.code-wrap\').querySelector(\'code\').textContent)">COPY</button></div><pre class="code-block"><code><span class="kw">from</span> pathlib <span class="kw">import</span> Path\n\n<span class="cm"># Oluşturma — Windows\'ta bile / operatörü</span>\np = <span class="fn">Path</span>(<span class="str">"data"</span>) / <span class="str">"raw"</span> / <span class="str">"corpus.txt"</span>\n<span class="fn">print</span>(p)         <span class="cm"># data/raw/corpus.txt  (Windows: data\\raw\\corpus.txt)</span>\n\n<span class="cm"># Yol bileşenleri</span>\n<span class="fn">print</span>(p.parent)     <span class="cm"># Path("data/raw")</span>\n<span class="fn">print</span>(p.name)       <span class="cm"># "corpus.txt"</span>\n<span class="fn">print</span>(p.stem)       <span class="cm"># "corpus"  (uzantisiz isim)</span>\n<span class="fn">print</span>(p.suffix)     <span class="cm"># ".txt"</span>\n<span class="fn">print</span>(p.parts)      <span class="cm"># ("data", "raw", "corpus.txt")</span>\n\n<span class="cm"># Sorgulayıcılar</span>\np.exists()            <span class="cm"># bool</span>\np.is_file()           <span class="cm"># bool</span>\np.is_dir()            <span class="cm"># bool</span>\n\n<span class="cm"># Oluştur / sil</span>\n<span class="fn">Path</span>(<span class="str">"output/logs"</span>).mkdir(parents=<span class="kw">True</span>, exist_ok=<span class="kw">True</span>)\np.unlink(missing_ok=<span class="kw">True</span>)   <span class="cm"># dosyayi sil, yoksa hata yok</span></code></pre></div>'

+ '<p class="l-text"><strong>Okuma ve yazma</strong> — Küçük dosyalar için tek satırlık çağrılar open/read/close üçlemesinin yerini alır:</p>'

+ '<div class="code-wrap"><div class="code-label"><span>PYTHON</span><button class="copy-btn" onclick="navigator.clipboard.writeText(this.closest(\'.code-wrap\').querySelector(\'code\').textContent)">COPY</button></div><pre class="code-block"><code><span class="kw">from</span> pathlib <span class="kw">import</span> Path\n\nconfig = <span class="fn">Path</span>(<span class="str">"config.json"</span>)\n\n<span class="cm"># Tüm dosyayı metin veya bayt olarak oku</span>\ntext = config.read_text(encoding=<span class="str">"utf-8"</span>)\nraw  = config.read_bytes()\n\n<span class="cm"># Yaz — açma/kapama senin yerine yapılır</span>\n<span class="fn">Path</span>(<span class="str">"output/result.txt"</span>).write_text(<span class="str">"bitti\\n"</span>, encoding=<span class="str">"utf-8"</span>)\n\n<span class="cm"># Glob — desene göre dosya bul, ** ile özyinelemeli</span>\nroot = <span class="fn">Path</span>(<span class="str">"data"</span>)\n<span class="kw">for</span> csv <span class="kw">in</span> root.glob(<span class="str">"**/*.csv"</span>):\n    <span class="fn">print</span>(csv, csv.stat().st_size)\n\n<span class="cm"># Uzantıyı string oyunu olmadan değiştir</span>\nsrc = <span class="fn">Path</span>(<span class="str">"model.ckpt"</span>)\ndst = src.with_suffix(<span class="str">".safetensors"</span>)   <span class="cm"># Path("model.safetensors")</span>\n\n<span class="cm"># Yolu mutlak biçime çevir</span>\n<span class="fn">print</span>(<span class="fn">Path</span>(<span class="str">"."</span>).resolve())</code></pre></div>'

+ '<div class="l-note"><strong>Geçiş kuralı:</strong> Parmakların <code>os.path.</code> yazmaya başlarsa dur. Ne istiyorsan büyük ihtimalle bir <code>Path</code> nesnesinde ve genellikle daha kısa adla mevcut. <code>os</code>\'nin tek galip olduğu yer: <code>os.environ</code> ile ortam değişkenleri.</div>'


/* ============================================================
   BÖLÜM 5: WALRUS OPERATÖRÜ
   ============================================================ */
+ '<h2 class="l-title">5. Walrus Operatörü — :=</h2>'

+ '<div class="calc-highlight">Walrus operatörü (<code>:=</code>, resmi adı "atama ifadesi", 3.8+) aynı ifade içinde hem değer atamana hem de o değeri kullanmana izin verir. Küçüktür ama en sinir bozucu kalıbı bitirir: bir şeyi çağır, sakla, hemen kontrol et.</div>'

+ '<p class="l-text"><strong>EOF\'a kadar oku</strong> — Klasik kullanım:</p>'

+ '<div class="code-wrap"><div class="code-label"><span>PYTHON</span><button class="copy-btn" onclick="navigator.clipboard.writeText(this.closest(\'.code-wrap\').querySelector(\'code\').textContent)">COPY</button></div><pre class="code-block"><code><span class="cm"># Önce — read iki kez, ikincisini unutmak kolay</span>\nchunk = f.read(<span class="num">4096</span>)\n<span class="kw">while</span> chunk:\n    process(chunk)\n    chunk = f.read(<span class="num">4096</span>)\n\n<span class="cm"># Sonra — tek çağrı, tek doğru kaynak</span>\n<span class="kw">while</span> chunk := f.read(<span class="num">4096</span>):\n    process(chunk)</code></pre></div>'

+ '<p class="l-text"><strong>Comprehension filtreleri</strong> — Hem filtrelemen hem de hesapladığın değeri kullanman gerektiğinde walrus yeniden hesaplamayı engeller:</p>'

+ '<div class="code-wrap"><div class="code-label"><span>PYTHON</span><button class="copy-btn" onclick="navigator.clipboard.writeText(this.closest(\'.code-wrap\').querySelector(\'code\').textContent)">COPY</button></div><pre class="code-block"><code><span class="cm"># KÖTÜ — expensive_score() her öğe için iki kez çağrılıyor</span>\nscored = [(x, expensive_score(x)) <span class="kw">for</span> x <span class="kw">in</span> items <span class="kw">if</span> expensive_score(x) > <span class="num">0.5</span>]\n\n<span class="cm"># İYİ — bir kez hesapla, hem filtrede hem sonuçta kullan</span>\nscored = [(x, s) <span class="kw">for</span> x <span class="kw">in</span> items <span class="kw">if</span> (s := expensive_score(x)) > <span class="num">0.5</span>]\n\n<span class="cm"># Regex eşle ve kullan, iç içe yapı yok</span>\n<span class="kw">import</span> re\n<span class="kw">if</span> m := re.search(<span class="str">r"id=(\\d+)"</span>, url):\n    user_id = m.group(<span class="num">1</span>)</code></pre></div>'

+ '<div class="l-note"><strong>Abartma:</strong> Walrus bir satır kazandırıyor ama bir isim daha ekliyorsa değer. Üç işlemi bir satıra tıkıyor ve okumak için yorum gerekiyorsa, sıkıcı sürümü yaz. Anlaşılırlık zekilikten önce gelir.</div>'


/* ============================================================
   BÖLÜM 6: GELİŞMİŞ F-STRINGS
   ============================================================ */
+ '<h2 class="l-title">6. f-string — Güçlü Özellikler</h2>'

+ '<div class="calc-highlight">f-string\'ler yıllar önce <code>.format()</code> ve <code>%</code> biçimlendirmesinin yerini aldı, ama çoğu kod kapasitelerinin yalnızca %10\'unu kullanır. Biçim spec mini dili sayıları, hizalamayı, debug çıktısını ve repr\'i tek satırda — çoğunlukla alternatiflerden daha hızlı — halleder.</div>'

+ '<div class="code-wrap"><div class="code-label"><span>PYTHON</span><button class="copy-btn" onclick="navigator.clipboard.writeText(this.closest(\'.code-wrap\').querySelector(\'code\').textContent)">COPY</button></div><pre class="code-block"><code>pi = <span class="num">3.14159265</span>\nname = <span class="str">"ada"</span>\n\n<span class="cm"># Ondalık kesinliği ve genişlik</span>\n<span class="fn">print</span>(<span class="fn">f</span><span class="str">"{pi:.2f}"</span>)        <span class="cm"># 3.14         — sabit nokta, 2 basamak</span>\n<span class="fn">print</span>(<span class="fn">f</span><span class="str">"{pi:.4e}"</span>)        <span class="cm"># 3.1416e+00   — bilimsel gosterim</span>\n<span class="fn">print</span>(<span class="fn">f</span><span class="str">"{pi:10.2f}"</span>)      <span class="cm"># "      3.14" — genislik 10, saga yasli</span>\n<span class="fn">print</span>(<span class="fn">f</span><span class="str">"{<span class="num">1234567</span>:,}"</span>)    <span class="cm"># 1,234,567    — binlik ayraci</span>\n<span class="fn">print</span>(<span class="fn">f</span><span class="str">"{<span class="num">0.875</span>:.1%}"</span>)     <span class="cm"># 87.5%        — yuzde biciminde</span>\n\n<span class="cm"># Hizalama: < sola, > saga, ^ ortaya, opsiyonel dolgu karakteri</span>\n<span class="fn">print</span>(<span class="fn">f</span><span class="str">"|{name:&lt;10}|"</span>)     <span class="cm"># |ada       |</span>\n<span class="fn">print</span>(<span class="fn">f</span><span class="str">"|{name:&gt;10}|"</span>)     <span class="cm"># |       ada|</span>\n<span class="fn">print</span>(<span class="fn">f</span><span class="str">"|{name:^10}|"</span>)     <span class="cm"># |   ada    |</span>\n<span class="fn">print</span>(<span class="fn">f</span><span class="str">"|{name:*^10}|"</span>)    <span class="cm"># |***ada****|</span>\n\n<span class="cm"># !r → repr(), !s → str() — loglarda altın değerinde</span>\nmsg = <span class="str">"hi\\tthere"</span>\n<span class="fn">print</span>(<span class="fn">f</span><span class="str">"alindi: {msg!r}"</span>)   <span class="cm"># alindi: \'hi\\tthere\'   — kacis karakterleri gorunur</span>\n\n<span class="cm"># 3.8+ kendini belgeleyen `=` — debug print için hayat kurtarıcı</span>\nx, y = <span class="num">7</span>, <span class="num">42</span>\n<span class="fn">print</span>(<span class="fn">f</span><span class="str">"{x=}, {y=}, {x+y=}"</span>)  <span class="cm"># x=7, y=42, x+y=49</span>\n\n<span class="cm"># Tarih biçimlendirme __format__ üzerinden</span>\n<span class="kw">from</span> datetime <span class="kw">import</span> datetime\nnow = datetime.now()\n<span class="fn">print</span>(<span class="fn">f</span><span class="str">"{now:%Y-%m-%d %H:%M}"</span>)  <span class="cm"># 2026-05-12 14:30</span></code></pre></div>'

+ '<div class="l-note">Debug f-string\'ler (<code>f"{var=}"</code>) bir fonksiyonu hızlıca incelemenin en hızlı yoludur. Birkaç tane bırak, çıktıyı oku, sil. <code>print("var =", var)</code>\'tan çok daha rahat ve <code>var</code>\'ı yeniden adlandırdığında bozulmadan hayatta kalır.</div>'


/* ============================================================
   BÖLÜM 7: __slots__
   ============================================================ */
+ '<h2 class="l-title">7. __slots__ — Bellek Tasarrufu</h2>'

+ '<div class="calc-highlight">Varsayılan olarak her Python örneği nitelikleri saklamak için bir <code>__dict__</code> taşır. Bu sözlük esnektir — istediğin anda alan ekleyebilirsin — ama örnek başına yaklaşık 100 bayta mâl olur. Milyonlarca örnek ürettiğin sınıflarda (token, kenar, parçacık) <code>__slots__</code> sözlüğü atıp nitelikleri sabit dizide tutar; örnek başına belleği %40-50 düşürür.</div>'

+ '<div class="code-wrap"><div class="code-label"><span>PYTHON</span><button class="copy-btn" onclick="navigator.clipboard.writeText(this.closest(\'.code-wrap\').querySelector(\'code\').textContent)">COPY</button></div><pre class="code-block"><code><span class="kw">import</span> sys\n\n<span class="kw">class</span> <span class="fn">PointA</span>:                <span class="cm"># varsayilan — __dict__ var</span>\n    <span class="kw">def</span> <span class="fn">__init__</span>(<span class="kw">self</span>, x, y):\n        <span class="kw">self</span>.x = x\n        <span class="kw">self</span>.y = y\n\n<span class="kw">class</span> <span class="fn">PointB</span>:                <span class="cm"># slot\'lu — sadece sabit nitelikler</span>\n    __slots__ = (<span class="str">"x"</span>, <span class="str">"y"</span>)\n    <span class="kw">def</span> <span class="fn">__init__</span>(<span class="kw">self</span>, x, y):\n        <span class="kw">self</span>.x = x\n        <span class="kw">self</span>.y = y\n\na = <span class="fn">PointA</span>(<span class="num">1</span>, <span class="num">2</span>)\nb = <span class="fn">PointB</span>(<span class="num">1</span>, <span class="num">2</span>)\n\n<span class="fn">print</span>(sys.getsizeof(a) + sys.getsizeof(a.__dict__))  <span class="cm"># ~152 bayt</span>\n<span class="fn">print</span>(sys.getsizeof(b))                              <span class="cm"># ~48 bayt</span>\n\n<span class="cm"># Bedel: yeni nitelik ekleyemezsin</span>\n<span class="cm"># b.z = 3  # AttributeError — \'z\' __slots__\'ta yok</span>\n\n<span class="cm"># Dataclass kısayolu — __slots__\'u senin için üretir (3.10+)</span>\n<span class="kw">from</span> dataclasses <span class="kw">import</span> dataclass\n<span class="kw">@dataclass</span>(slots=<span class="kw">True</span>)\n<span class="kw">class</span> <span class="fn">PointC</span>:\n    x: <span class="ty">float</span>\n    y: <span class="ty">float</span></code></pre></div>'

/* --- Plotly: Bellek karşılaştırma --- */
+ '<div class="calc-graph"><div class="cg-title">Örnek Başına Bellek: Düz Sınıf ve __slots__</div></div>'
+ '<div id="plot-python7-slots-tr" class="plotly-graph"></div>'
+ '<script>setTimeout(function(){'
+ '(function(){'
+ 'var labels = ["Düz sınıf (+ __dict__)", "__slots__ sınıfı", "@dataclass(slots=True)"];'
+ 'var bytes = [152, 48, 48];'
+ 'var colors = ["#ff6b6b", "#4ecdc4", "#c8a96e"];'
+ 'var trace = {x:labels, y:bytes, type:"bar", marker:{color:colors}, text:bytes.map(function(b){return b+" B";}), textposition:"auto"};'
+ 'var layout = {paper_bgcolor:"transparent", plot_bgcolor:"transparent",'
+ 'font:{color:"#ccc", size:14}, yaxis:{title:"Örnek başına bayt", gridcolor:"rgba(255,255,255,0.07)"},'
+ 'xaxis:{gridcolor:"rgba(255,255,255,0.07)"},'
+ 'margin:{l:60,r:20,t:10,b:60}};'
+ 'Plotly.newPlot("plot-python7-slots-tr",[trace],layout,{responsive:true});'
+ '})();'
+ '},250)</script>'

+ '<div class="l-note"><strong>Önemli olduğu yer:</strong> Milyon tokenlı bir tokenizer. Milyonlarca düğümlü bir graf. Milyonlarca parçacıklı bir simülasyon. On nesne için <code>__slots__</code> gürültüdür. On milyon için RAM\'e sığmakla diske swap olmak arasındaki farktır.</div>'


/* ============================================================
   BÖLÜM 8: PROTOCOL — YAPISAL TİPLEME
   ============================================================ */
+ '<h2 class="l-title">8. typing.Protocol — Kontrolü Olan Duck Typing</h2>'

+ '<div class="calc-highlight">Python hep duck typing yaptı: "vaklıyorsa ördektir." Eksik olan, tip denetleyicinin "şu metoda sahip her şey" diyebilmesiydi. <code>Protocol</code> (PEP 544, 3.8+) bunu çözer: şekli tanımlarsın, uyan her sınıf <em>kalıtım olmadan</em> ona uyar.</div>'

+ '<div class="code-wrap"><div class="code-label"><span>PYTHON</span><button class="copy-btn" onclick="navigator.clipboard.writeText(this.closest(\'.code-wrap\').querySelector(\'code\').textContent)">COPY</button></div><pre class="code-block"><code><span class="kw">from</span> typing <span class="kw">import</span> Protocol\n\n<span class="kw">class</span> <span class="fn">SupportsRead</span>(Protocol):\n    <span class="kw">def</span> <span class="fn">read</span>(<span class="kw">self</span>, n: <span class="ty">int</span> = -<span class="num">1</span>) -> <span class="ty">str</span>: ...\n\n<span class="kw">def</span> <span class="fn">load_first_kb</span>(source: SupportsRead) -> <span class="ty">str</span>:\n    <span class="kw">return</span> source.read(<span class="num">1024</span>)\n\n<span class="cm"># `read(n)` metodu olan her nesne işe yarar — kalıtım gerekmez</span>\n<span class="kw">from</span> io <span class="kw">import</span> StringIO\nbuf = <span class="fn">StringIO</span>(<span class="str">"merhaba dunya"</span>)\n<span class="fn">print</span>(<span class="fn">load_first_kb</span>(buf))           <span class="cm"># "merhaba dunya"</span>\n\n<span class="kw">with</span> <span class="fn">open</span>(<span class="str">"data.txt"</span>) <span class="kw">as</span> f:\n    <span class="fn">load_first_kb</span>(f)                  <span class="cm"># protokole o da uyar</span>\n\n<span class="cm"># Çalışma zamanında kontrol edilebilir varyant</span>\n<span class="kw">from</span> typing <span class="kw">import</span> runtime_checkable\n\n<span class="kw">@runtime_checkable</span>\n<span class="kw">class</span> <span class="fn">Sized</span>(Protocol):\n    <span class="kw">def</span> <span class="fn">__len__</span>(<span class="kw">self</span>) -> <span class="ty">int</span>: ...\n\n<span class="fn">print</span>(<span class="fn">isinstance</span>([<span class="num">1</span>, <span class="num">2</span>, <span class="num">3</span>], <span class="fn">Sized</span>))   <span class="cm"># True — list __len__ a sahip</span>\n<span class="fn">print</span>(<span class="fn">isinstance</span>(<span class="num">42</span>, <span class="fn">Sized</span>))           <span class="cm"># False — int __len__ a sahip değil</span></code></pre></div>'

+ '<p class="l-text"><code>collections.abc</code> hazır protokollerle gelir: <code>Iterable</code>, <code>Iterator</code>, <code>Sized</code>, <code>Container</code>, <code>Hashable</code>, <code>Mapping</code>, <code>Sequence</code>. Önce onlara bak. Kendi <code>Protocol</code>\'ünü, alanına özel bir şekle ihtiyacın olduğunda yaz — örneğin generic bir model değerlendirici için "<code>.predict(X)</code> metoduna sahip olan her şey".</p>'

+ '<div class="l-note"><strong>Çıkarım:</strong> Yetişkin Python kod tabanlarının duck typing esnekliği ile statik kontrol güvenliğini bir arada elde ettiği yer Protocol\'lerdir. Somut kalıtım (<code>class Cat(Animal)</code>) "bir tür"; protokoller "buna benziyor" için.</div>'


/* ============================================================
   BÖLÜM 9: MODERN PAKETLEME
   ============================================================ */
+ '<h2 class="l-title">9. Modern Paketleme — pyproject.toml + uv</h2>'

+ '<div class="calc-highlight">On yıl boyunca "bunu nasıl paketlerim?" sorusunun cevabı <code>setup.py</code>, <code>requirements.txt</code>, <code>setup.cfg</code>, <code>MANIFEST.in</code> ve etkinleştirmeyi unuttuğun bir sanal ortam içeriyordu. Modern cevap bir dosya (<code>pyproject.toml</code>) ve bir araç (<code>uv</code>).</div>'

+ '<p class="l-text"><strong>pyproject.toml</strong> — proje metaverisinin, bağımlılıkların ve araç ayarlarının tek doğru kaynağı:</p>'

+ '<div class="code-wrap"><div class="code-label"><span>TOML</span><button class="copy-btn" onclick="navigator.clipboard.writeText(this.closest(\'.code-wrap\').querySelector(\'code\').textContent)">COPY</button></div><pre class="code-block"><code><span class="cm">[project]</span>\nname = <span class="str">"nlp-pipeline"</span>\nversion = <span class="str">"0.1.0"</span>\ndescription = <span class="str">"Külliyatları temizler ve tokenize eder."</span>\nreadme = <span class="str">"README.md"</span>\nrequires-python = <span class="str">"&gt;=3.11"</span>\ndependencies = [\n    <span class="str">"numpy&gt;=2.0"</span>,\n    <span class="str">"pandas&gt;=2.2"</span>,\n    <span class="str">"transformers&gt;=4.40"</span>,\n]\n\n[project.optional-dependencies]\ndev = [<span class="str">"pytest"</span>, <span class="str">"ruff"</span>, <span class="str">"mypy"</span>]\n\n[project.scripts]\nrun-pipeline = <span class="str">"nlp_pipeline.cli:main"</span>\n\n[build-system]\nrequires = [<span class="str">"hatchling"</span>]\nbuild-backend = <span class="str">"hatchling.build"</span>\n\n[tool.ruff]\nline-length = 100\ntarget-version = <span class="str">"py311"</span>\n\n[tool.mypy]\nstrict = <span class="kw">true</span></code></pre></div>'

+ '<p class="l-text"><strong>uv — yeni paket yöneticisi.</strong> Rust tabanlı, pip\'ten 10-100 kat hızlı, hem Python sürümünü hem sanal ortamı tek araçla yönetir:</p>'

+ '<div class="code-wrap"><div class="code-label"><span>SHELL</span><button class="copy-btn" onclick="navigator.clipboard.writeText(this.closest(\'.code-wrap\').querySelector(\'code\').textContent)">COPY</button></div><pre class="code-block"><code><span class="cm"># Yeni projeyi başlat — pyproject.toml + .venv oluşturur</span>\nuv init nlp-pipeline\n<span class="kw">cd</span> nlp-pipeline\n\n<span class="cm"># Bağımlılık ekle — pyproject.toml\'i günceller VE yükler</span>\nuv add numpy pandas transformers\nuv add --dev pytest ruff mypy\n\n<span class="cm"># Komutu proje venv\'inde çalıştır (manuel aktivasyon yok)</span>\nuv run python train.py\nuv run pytest\n\n<span class="cm"># Üretilebilir kurulum için bağımlılıkları kilitle</span>\nuv lock                <span class="cm"># uv.lock yazar</span>\nuv sync                <span class="cm"># uv.lock\'taki sürümleri tam olarak kurar</span>\n\n<span class="cm"># Farklı bir Python sürümü kur</span>\nuv python install 3.12\nuv venv --python 3.12\n\n<span class="cm"># Tek seferlik script\'i izole bağımlılıklarla çalıştır (proje gerekmez)</span>\nuv run --with rich --with httpx scratch.py</code></pre></div>'

+ '<div class="l-note"><strong>Geçiş kopya kâğıdı:</strong> <code>pip install X</code> &rarr; <code>uv add X</code>. <code>python -m venv .venv</code> &rarr; <code>uv venv</code>. <code>pip freeze &gt; requirements.txt</code> &rarr; <code>uv lock</code>. <code>python script.py</code> &rarr; <code>uv run script.py</code>. Zihinsel model: <em>uv ortamın sahibi, sen sadece ne istediğini bildirirsin</em>.</div>'


/* ============================================================
   BÖLÜM 10: 2026 EN İYİ PRATİKLER
   ============================================================ */
+ '<h2 class="l-title">10. 2026 Python Araç Zinciri</h2>'

+ '<div class="calc-highlight">Modern bir Python projesi sadece koddan ibaret değildir — kod artı sorunları insanlardan önce yakalayan küçük bir araç ordusudur. Aşağıdaki liste fiili standarttır. Bir kez yapılandır, ayarları commit\'le, bir hatadan seni kurtarana kadar bir daha düşünme.</div>'

+ '<div class="calc-cards">'
+ '<div class="calc-card"><div class="cc-icon">uv</div><div class="cc-title">Ortam ve bağımlılık</div><div class="cc-body"><code>uv</code> pip, pip-tools, venv, pyenv ve pipx\'i tek başına temsil eder. Tek araç, tek konfig, ışık hızında.</div></div>'
+ '<div class="calc-card"><div class="cc-icon">ruff</div><div class="cc-title">Lint + Format</div><div class="cc-body"><code>ruff check</code> flake8/isort/pylint yerine. <code>ruff format</code> black yerine. Rust hızında, <code>pyproject.toml</code>\'da tek konfig.</div></div>'
+ '<div class="calc-card"><div class="cc-icon">mypy</div><div class="cc-title">Statik tip kontrolü</div><div class="cc-body"><code>mypy --strict</code> ya da <code>pyright</code>. CI\'da çalıştır. İnsan gözünün güvenilir biçimde yakalayamadığı hataları yakalar.</div></div>'
+ '<div class="calc-card"><div class="cc-icon">pytest</div><div class="cc-title">Test</div><div class="cc-body"><code>pytest</code> + fixture + parametrize. Kapsam için <code>pytest-cov</code>. Property testleri için <code>hypothesis</code>.</div></div>'
+ '<div class="calc-card"><div class="cc-icon">pre</div><div class="cc-title">pre-commit</div><div class="cc-body">Her commit öncesi <code>ruff</code>, <code>mypy</code>, gizli tarayıcısını çalıştırır. Kötü kodu daha kapıda durdurur.</div></div>'
+ '<div class="calc-card"><div class="cc-icon">CI</div><div class="cc-title">GitHub Actions</div><div class="cc-body">Tek bir workflow: <code>uv</code> kur, <code>uv sync</code>, <code>uv run ruff check</code>, <code>uv run mypy</code>, <code>uv run pytest</code>. Klondan karara beş dakika.</div></div>'
+ '</div>'

+ '<p class="l-text"><strong>Minimal <code>ruff</code> + <code>mypy</code> ayarı (<code>pyproject.toml</code>\'a ekle):</strong></p>'

+ '<div class="code-wrap"><div class="code-label"><span>TOML</span><button class="copy-btn" onclick="navigator.clipboard.writeText(this.closest(\'.code-wrap\').querySelector(\'code\').textContent)">COPY</button></div><pre class="code-block"><code>[tool.ruff]\nline-length = 100\ntarget-version = <span class="str">"py311"</span>\n\n[tool.ruff.lint]\nselect = [<span class="str">"E"</span>, <span class="str">"F"</span>, <span class="str">"W"</span>, <span class="str">"I"</span>, <span class="str">"N"</span>, <span class="str">"UP"</span>, <span class="str">"B"</span>, <span class="str">"SIM"</span>, <span class="str">"RUF"</span>]\nignore = [<span class="str">"E501"</span>]   <span class="cm"># satir uzunlugu formatlayicinin isi</span>\n\n[tool.mypy]\npython_version = <span class="str">"3.11"</span>\nstrict = <span class="kw">true</span>\nwarn_unused_ignores = <span class="kw">true</span>\nwarn_redundant_casts = <span class="kw">true</span>\n\n[tool.pytest.ini_options]\ntestpaths = [<span class="str">"tests"</span>]\naddopts = <span class="str">"-ra --strict-markers"</span></code></pre></div>'

+ '<div class="think-box"><strong>Profesyonellik açığı:</strong> Kendini yetiştiren birçok Python kullanıcısı bu araçları hiç benimsemez — "benim makinemde çalışıyor" yetiyormuş gibi gelir. Gerçek bir takıma katıldığın ya da gerçek kullanıcılara dağıttığın anda bu zincir artık opsiyonel olmaz. İyi haber: yukarıdaki snippet\'la bir kez ayarla, o günden sonra disiplini araçlar senin yerine uygular.</div>'

+ '<div class="l-note"><strong>Python L7 sonu.</strong> Artık Python\'u 2026 geliştiricisi gibi yazıyorsun: <code>__init__</code> kalıbı yerine dataclass, <code>if/elif</code> merdiveni yerine <code>match</code>, <code>|</code> ve <code>list[int]</code> ile tip ipuçları, <code>os.path</code> yerine <code>pathlib</code>, pip yerine <code>uv</code> ve hataları commit landinglerden önce yakalayan bir araç zinciri. Bu alışkanlıkları her projeye taşı — bileşik etki yaparlar.</div>'

};
